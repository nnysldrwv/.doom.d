#+title: Doom Emacs Config
#+author: Sean Yuan
#+property: header-args:emacs-lisp :tangle yes :cache yes :results silent :padline no

* Bootstrap
** init.el
#+begin_src emacs-lisp :tangle init.el
;;; init.el -*- lexical-binding: t; -*-

;; Doom Emacs module selection.
;; SPC h d m  to browse available modules and their flags.
;; doom/reload after editing, or run 'doom sync' from CLI.

(doom! :input

       :completion
       corfu                          ; babel 代码块里仍需要补全
       (vertico +icons)

       :ui
       doom
       hl-todo
       modeline
       nav-flash
       ophints
       (popup +all +defaults)
       ;; treemacs
       window-select
       unicode
       zen

       :editor
       (evil +everywhere)
       file-templates
       fold
       snippets
       word-wrap
       ;; 已移除: multiple-cursors (写中文笔记几乎用不上)
       ;; 已移除: (format +onsave)  (写作场景不需要保存时格式化)

       :emacs
       undo
       (ibuffer +icons)
       (dired +dirvish +icons)

       ;; 已移除: :checkers syntax (写作不需要 flycheck/flymake)

       :tools
       (eval +overlay)                ; 保留: org-babel 结果 overlay
       magit
       pdf
       ;; 已移除: lookup (主要服务于代码跳转)

       :os
       (:if (featurep :system 'windows) windows)

       :lang
       emacs-lisp                     ; 自己写配置 + elisp babel
       markdown
       (org
        +dragndrop
        +present
        +noter
        +pandoc)
       python                         ; 保留以支持 babel (去掉 +lsp)
       sh                             ; babel 经常跑 shell
       yaml                           ; 轻量, 写 frontmatter/CI 有用
       latex

       :app
       (calendar +org-gcal)
       (rss +org +youtube)

       :config
       literate
       (default +bindings +smartparens))
#+end_src

** packages.el
#+begin_src emacs-lisp :tangle packages.el
;; -*- no-byte-compile: t; -*-
;;; $DOOMDIR/packages.el
(package! cal-china-x)
(package! nov)
(package! powershell)
(package! sis)

;; denote
(package! denote)
(package! denote-org)
(package! denote-journal)
(package! denote-markdown)
(package! consult-notes
  :recipe (:host github :repo "mclear-tools/consult-notes"))

;; agent shell (高频使用, 保留)
(package! shell-maker)
(package! acp)
(package! agent-shell)

;; performance
(package! gcmh)

;; theme
(package! ef-themes)

;; writing & display
;; (package! olivetti)
(package! org-appear
  :recipe (:host github :repo "awth13/org-appear"))
(package! pangu-spacing)
(package! spacious-padding)
(package! ultra-scroll
  :recipe (:host github :repo "jdtsmith/ultra-scroll"))
;; 已移除: rainbow-identifiers (彩色变量名, 写代码用, 写笔记无意义)
;; 已移除: dired-narrow (dirvish 自带 dirvish-narrow)

;; 完美解决中英文字符在表格中对齐的问题
(package! valign)

;; 快速跳转到任意位置, 通过汉字拼音的方式
(package! ace-pinyin
  :recipe (:host github :repo "cute-jumper/ace-pinyin"))

;; ---- 新增: 写作 / 笔记 / 工作流增强 ----

;; Org 视觉现代化: 标题/TODO/tag/checkbox/timestamp 美化
;; 与 valign 协同: 禁用 org-modern 的表格渲染, 交给 valign
;; (package! org-modern)

;; 可视化 undo 树, 比 Doom 默认 undo-fu 更直观
(package! vundo)

;; 高速增量拼写检查 (Windows/MSYS2 需要 pkg-config 能找到 enchant-2)
(package! jinx)

;; Agenda 视图按组分块, 比堆 tags-todo 漂亮且易维护
(package! org-super-agenda)

;; 跨笔记内容嵌入
(package! org-transclusion)

;; 强大的 org 查询语言, 比 org-agenda-custom-commands 灵活
(package! org-ql)

;; Elfeed Tube live transcript integration with mpv.
(package! elfeed-tube-mpv :pin "2d7251d8363661f7bae3fd6b27ef88edc65d25ed")

;; 复式记账 / 投资记录 (配套 CLI: pip install beancount fava)
(package! beancount
  :recipe (:host github :repo "beancount/beancount-mode"))

(package! org-fragtog)
#+end_src

** Speed (gcmh)
#+begin_src emacs-lisp
;;; config.el -*- lexical-binding: t; -*-

;; GCMH: idle 时回收 GC, 输入时提高阈值避免卡顿
;; 256MB 在 idle 回收时会有明显停顿, 64MB 更平滑
(use-package! gcmh
  :init
  (setq gcmh-idle-delay 5
        gcmh-high-cons-threshold (* 64 1024 1024))  ; 64MB during idle
  :config
  (gcmh-mode 1))
#+end_src

* Personal
** Identity
#+begin_src emacs-lisp
(setq user-full-name "Sean Yuan"
      user-mail-address "yuanxiang424@gmail.com")
#+end_src

** Secrets
Loaded early so downstream config (org-gcal, agent-shell …) sees the credentials.
#+begin_src emacs-lisp
(load! "secrets" doom-user-dir t)
#+end_src

* Core Defaults
** Misc
#+begin_src emacs-lisp
(setq confirm-kill-emacs nil       ; 关闭 emacs 时无需额外确认
      system-time-locale "C"       ; 设置系统时间显示方式
      scroll-margin 2)             ; 保留少量滚动边距

;; 删除文件先进垃圾筒
(setq delete-by-moving-to-trash t)

(setq word-wrap-by-category t)

;; 在 Org mode 中禁用自适应换行缩进, 实现左对齐
(add-hook 'org-mode-hook (lambda () (adaptive-wrap-prefix-mode -1)))

(setq initial-major-mode 'org-mode)
(setq initial-scratch-message nil)

;; Smooth mouse scrolling
(setq mouse-wheel-scroll-amount '(2 ((shift) . 1))
      mouse-wheel-progressive-speed nil
      mouse-wheel-follow-mouse t
      scroll-step 1)

;; so-long-mode: 智能处理超长单行 (minified JS / 单行大 JSON 等)
;; 比按文件大小一刀切禁用 font-lock 更聪明: 只有"行过长"才介入
(global-so-long-mode 1)
#+end_src

** Encoding
#+begin_src emacs-lisp
(prefer-coding-system 'utf-8)
(set-default-coding-systems 'utf-8)
(setq default-buffer-file-coding-system 'utf-8)
#+end_src

** Quiet
#+begin_src emacs-lisp
(setq byte-compile-warnings '(not obsolete))
(setq warning-suppress-log-types '((comp) (bytecomp)))
(setq native-comp-async-report-warnings-errors 'silent)
(setq inhibit-startup-echo-area-message (user-login-name))
(setq visible-bell t)
(setq ring-bell-function 'ignore)
(setq set-message-beep 'silent)
#+end_src

** Editing: CJK join fix
#+begin_src emacs-lisp
;; J 合并行时, 中文之间不插入多余空格
(defun my/evil-join-cjk-advice (&rest _)
  "Remove unwanted space between CJK characters after evil-join."
  (save-excursion
    (beginning-of-line)
    (while (re-search-forward "\\(\\cc\\) \\(\\cc\\)" (line-end-position) t)
      (replace-match "\\1\\2"))))

(advice-add 'evil-join :after #'my/evil-join-cjk-advice)
#+end_src

** Delete file link + underlying attachment (shared helper)
#+begin_src emacs-lisp
;; 删除文件链接时同时删除附件文件 (图片/PDF/EPUB 等) —— 共享确认/删除逻辑
(defun my/delete-file-link--try-delete (abspath)
  "Try to delete ABSPATH after user confirmation.
Returns 'deleted, 'link-only, or nil."
  (cond
   ((file-exists-p abspath)
    (if (y-or-n-p (format "Delete \"%s\"? " (file-name-nondirectory abspath)))
        (progn (delete-file abspath) 'deleted)
      (message "Cancelled") nil))
   ((y-or-n-p (format "File \"%s\" not found. Remove link? "
                      (file-name-nondirectory abspath)))
    'link-only)
   (t nil)))
#+end_src

** Windows-specific Performance
VC, process creation, file I/O, rendering. 大文件渲染已交给 so-long-mode 处理。
#+begin_src emacs-lisp
(when (featurep :system 'windows)
  ;; VC / Git
  (setq auto-revert-check-vc-info nil
        auto-revert-interval 10
        vc-handled-backends '(Git)
        vc-git-annotate-switches "-w")

  ;; Process creation
  (setq w32-pipe-read-delay 0
        w32-pipe-buffer-size (* 64 1024)
        process-adaptive-read-buffering nil)

  ;; File I/O
  (setq inhibit-compacting-font-caches t
        w32-get-true-file-attributes nil)

  ;; Rendering (only what Doom doesn't set)
  (setq jit-lock-defer-time 0.05)

  ;; Long lines (Emacs 29+)
  (when (boundp 'long-line-threshold)
    (setq long-line-threshold 1000
          large-hscroll-threshold 1000
          syntax-wholeline-max 1000))

  ;; 已移除: 按 buffer-size > 512KB 切到 fundamental-mode 的 hook
  ;; (so-long-mode 已经更智能地处理长行问题)

  ;; pdf-tools: MSYS2 epdfinfo
  (setenv "PATH" (concat "C:\\Users\\fengxing.chen\\scoop\\apps\\msys2\\current\\mingw64\\bin" ";" (getenv "PATH")))
  (setq pdf-info-epdfinfo-program "C:\\Users\\fengxing.chen\\scoop\\apps\\msys2\\current\\mingw64\\bin\\epdfinfo.exe")

  ;; project.el / grep / locate all call bare `find`, which on Windows PATH
  ;; resolves to C:\Windows\System32\FIND.EXE — a completely different tool.
  (let ((gnu-find "C:/Program Files/Git/usr/bin/find.exe"))
    (when (file-executable-p gnu-find)
      (setq find-program gnu-find))))
#+end_src

* Appearance
** Theme & UI
#+begin_src emacs-lisp
(setq doom-theme 'ef-spring)

(use-package! doom-modeline
  :custom
  (doom-modeline-buffer-encoding nil)
  (doom-modeline-enable-word-count nil))

;; 全局 visual line
(global-visual-line-mode)

;; j/k 按视觉行移动 —— evil-define-motion 确保 visual state (v/V/C-v) 都正常
(evil-define-motion my/evil-next-visual-line (count)
  "Move COUNT lines down, respecting `visual-line-mode' when active."
  :type line
  (let ((line-move-visual (and visual-line-mode t)))
    (evil-line-move (or count 1))))

(evil-define-motion my/evil-previous-visual-line (count)
  "Move COUNT lines up, respecting `visual-line-mode' when active."
  :type line
  (let ((line-move-visual (and visual-line-mode t)))
    (evil-line-move (- (or count 1)))))

(after! evil
  (define-key evil-motion-state-map "j" #'my/evil-next-visual-line)
  (define-key evil-motion-state-map "k" #'my/evil-previous-visual-line)
  (define-key evil-visual-state-map "j" #'my/evil-next-visual-line)
  (define-key evil-visual-state-map "k" #'my/evil-previous-visual-line))

(setq display-line-numbers-type nil
      use-short-answers t)

(fringe-mode '(0 . 0)) ;; No fringe

;; 指定启动时的窗口位置和大小
(setq initial-frame-alist '((top . 137)
                            (left . 657)
                            (width . 81)
                            (height . 38)))

;; 新开窗口时默认是左右结构
(setq split-height-threshold nil)
(setq split-width-threshold 0)

(menu-bar-mode -1)
(tool-bar-mode -1)
(scroll-bar-mode -1)

(setq-default x-stretch-cursor t
              x-underline-at-descent-line t)
#+end_src

** Fonts
方案 1: 拉丁用 Ubuntu Mono, 汉字用 Sarasa Mono SC。
方案 2: 拉丁用 Jetbrains Mono, 汉字用 Sarasa Mono SC。
#+begin_src emacs-lisp
;; Doom 自带的字体变量配置
(setq doom-font (font-spec :family "Ubuntu Mono" :size 18)
      doom-big-font (font-spec :family "Jetbrains Mono" :size 36)
      doom-serif-font doom-font
      doom-symbol-font (font-spec :family "Sarasa Mono SC")
      ;; doom-variable-pitch-font (font-spec :family "Ubuntu Mono" :weight 'bold))
      doom-variable-pitch-font (font-spec :family "Jetbrains Mono" :weight 'bold))
(setq use-default-font-for-symbols nil)

;; Doom 的字体加载顺序问题: 必须在 after-setting-font-hook 里设, 否则被覆盖
(add-hook! 'after-setting-font-hook
  ;; === 关键: 给 CJK 脚本指定 Sarasa Mono SC ===
  (set-fontset-font t 'han      (font-spec :family "Sarasa Mono SC"))
  (set-fontset-font t 'cjk-misc (font-spec :family "Sarasa Mono SC"))
  (set-fontset-font t 'kana     (font-spec :family "Sarasa Mono SC"))
  (set-fontset-font t 'hangul   (font-spec :family "Sarasa Mono SC"))
  ;; === 符号 / 数学 / emoji ===
  (set-fontset-font t 'symbol       (font-spec :family "Symbola"))
  (set-fontset-font t 'mathematical (font-spec :family "Symbola"))
  (set-fontset-font t 'emoji        (font-spec :family "Segoe UI Emoji")))
#+end_src

* Editor Enhancements
** ace-window
#+begin_src emacs-lisp
(use-package! ace-window
  :config
  (setq aw-keys '(?a ?s ?d ?f ?g ?h ?j ?k ?l)
        aw-scope 'frame
        aw-background t)

  ;; 覆盖默认的 other-window (C-x o)
  (global-set-key [remap other-window] #'ace-window)

  (setq aw-display-mode-overlay t
        aw-leading-char-style 'char)

  (after! evil
    (define-key evil-motion-state-map (kbd "C-w w") #'ace-window)
    (define-key evil-normal-state-map (kbd "C-w w") #'ace-window)))
#+end_src

** vundo (visual undo tree)
#+begin_src emacs-lisp
;; vundo: 可视化 undo 树
;; - Doom 默认 undo-fu 没有树形视图
;; - 触发后用 hjkl 浏览历史分支, q 退出
(use-package! vundo
  :commands vundo
  :config
  (setq vundo-glyph-alist vundo-unicode-symbols))
#+end_src
** evil-escape
#+begin_src emacs-lisp
(defun my/evil-escape-only-in-insert-state ()
  "只有在 evil insert state 时才允许 evil-escape 生效。"
  (not (evil-insert-state-p)))

(after! evil-escape
  (setq evil-escape-key-sequence "jk"
        ;; 默认就是 0.1；对 jk 来说这个值通常比较合适，也更不容易误触
        evil-escape-delay 0.10
        ;; 只允许 jk，不允许 kj，减少误触发
        evil-escape-unordered-key-sequence nil)

  ;; 在终端类 buffer 里禁用
  (dolist (mode '(vterm-mode term-mode))
    (add-to-list 'evil-escape-excluded-major-modes mode))

  ;; 只在 insert state 生效
  (add-to-list 'evil-escape-inhibit-functions
               #'my/evil-escape-only-in-insert-state))
#+end_src

* Org-mode
** Core
#+begin_src emacs-lisp
(setq org-directory "~/org")

(after! org
  (require 'org-protocol)
  (require 'org-habit)

  (setq org-habit-graph-column 50
        org-habit-preceding-days 21
        org-habit-following-days 7
        org-habit-show-habits-only-for-today nil)

  ;; ---- TODO keywords ----
  (setq org-todo-keywords
        '((sequence "TODO(t)" "NEXT(n)" "PROG(p!)" "WAIT(w@/!)" "|" "DONE(d!)" "FAIL(f@)")))
  (setq org-todo-keyword-faces
        '(("TODO" :foreground "#2952a3" :weight bold)
          ("NEXT" :foreground "#e67e22" :weight bold)
          ("PROG" :foreground "#c0392b" :weight bold)
          ("WAIT" :foreground "#8b6914" :weight bold)
          ("DONE" :foreground "#2e7d32" :weight bold)
          ("FAIL" :foreground "#9e9e9e" :weight bold)))

  (setq org-log-done 'time
        org-log-into-drawer t)

  ;; ---- Display ----
  (setq org-confirm-babel-evaluate nil
        org-return-follows-link t
        org-startup-folded 'content
        org-hide-emphasis-markers t
        org-ellipsis " ▾")

  ;; Inline images
  (setq image-use-external-converter t
        org-image-actual-width '(600))

  (custom-set-variables '(org-startup-indented nil))

  ;; 抄的 ljg 的: babel 执行后重新显示 inline image
  (add-hook! 'org-babel-after-execute-hook 'org-display-inline-images 'append)

  ;; Fix table truncation: use tabular + adjustbox auto-fit
  (setq org-latex-default-table-environment "tabular"))
#+end_src

** Display: org-modern / spacious-padding / olivetti / ultra-scroll / pangu-spacing / org-appear
#+begin_src emacs-lisp
(after! org
  (use-package! spacious-padding
    :custom (line-spacing 3)
    :init (spacious-padding-mode 1))

  (use-package! pangu-spacing
    :config
    (global-pangu-spacing-mode 1)
    (setq pangu-spacing-real-insert-separtor t))

  ;; (use-package! olivetti
  ;;   :hook (org-mode . olivetti-mode)
  ;;   :config (setq olivetti-body-width 98))

  (use-package! ultra-scroll
    :init
    (setq scroll-conservatively 101
          scroll-margin 0)
    :config
    (ultra-scroll-mode 1))

  (use-package! org-appear
    :hook (org-mode . org-appear-mode)
    :config
    (setq org-appear-autoemphasis t
          org-appear-autosubmarkers t
          org-appear-autolinks nil))

  (use-package! valign
    :config
    (setq valign-fancy-bar t)
    (add-hook 'org-mode-hook #'valign-mode))

  (use-package! ace-pinyin
    :config
    (ace-pinyin-global-mode +1)))

  ;; org-modern: 现代化的 org 视觉
  ;; - 标题前缀、TODO 胶囊、tag、checkbox、timestamp 全部美化
  ;; - 表格让给 valign 处理 (org-modern 的表格渲染与 valign 冲突)
  ;; (use-package! org-modern
  ;;   :hook (org-mode . org-modern-mode)
  ;;   :hook (org-agenda-finalize . org-modern-agenda)
  ;;   :config
  ;;   (setq org-modern-table nil          ; 表格交给 valign
  ;;         org-modern-hide-stars nil
  ;;         org-modern-todo t
  ;;         org-modern-tag t
  ;;         org-modern-timestamp t
  ;;         org-modern-priority t
  ;;         org-modern-list '((?+ . "•") (?- . "–") (?* . "▸")))))
#+end_src

** org-attach
#+begin_src emacs-lisp
(after! org
  (require 'org-attach)
  (setq org-attach-id-dir (expand-file-name "data/" org-directory)
        org-attach-method 'cp
        org-attach-use-inheritance t
        org-attach-store-link-p 'attached
        org-attach-auto-tag nil)

  ;; Fix attachment 链接中文乱码
  (defun my/org-attach-store-link-decoded (&rest _)
    "Fix stored links from `org-attach-attach' to include decoded description."
    (when org-stored-links
      (let ((latest (car org-stored-links)))
        (when (and (stringp (car latest))
                   (string-prefix-p "attachment:" (car latest))
                   (or (null (cadr latest)) (string= (cadr latest) "")))
          (setcar (cdr latest)
                  (decode-coding-string
                   (url-unhex-string
                    (file-name-nondirectory
                     (substring (car latest) (length "attachment:"))))
                   'utf-8))))))
  (advice-add 'org-attach-attach :after #'my/org-attach-store-link-decoded))
#+end_src

** CJK emphasis fix (from ljg)
#+begin_src emacs-lisp
(after! org
  ;; @Eli 帮忙写的解决标记符号前后空格问题的代码
  (setq org-emphasis-regexp-components '("-[:space:]('\"{[:nonascii:]"
                                         "-[:space:].,:!?;'\")}\\[[:nonascii:]"
                                         "[:space:]"
                                         "."
                                         1))
  (setq org-match-substring-regexp
        (concat
         "\\([0-9a-zA-Zα-γΑ-Ω]\\)\\([_^]\\)\\("
         "\\(?:" (org-create-multibrace-regexp "{" "}" org-match-sexp-depth) "\\)"
         "\\|"
         "\\(?:" (org-create-multibrace-regexp "(" ")" org-match-sexp-depth) "\\)"
         "\\|"
         "\\(?:\\*\\|[+-]?[[:alnum:].,\\]*[[:alnum:]]\\)\\)"))
  (org-set-emph-re 'org-emphasis-regexp-components org-emphasis-regexp-components)
  (org-element-update-syntax)

  ;; 标记字符前后空格优化问题
  (defun eli/org-do-emphasis-faces (limit)
    "Run through the buffer and emphasize strings."
    (let ((quick-re (format "\\([%s]\\|^\\)\\([~=*/_+]\\)"
                            (car org-emphasis-regexp-components))))
      (catch :exit
        (while (re-search-forward quick-re limit t)
          (let* ((marker (match-string 2))
                 (verbatim? (member marker '("~" "="))))
            (when (save-excursion
                    (goto-char (match-beginning 0))
                    (and
                     (not (save-excursion
                            (forward-char 1)
                            (get-pos-property (point) 'org-emphasis)))
                     (not (org-match-line
                           "^[    ]*:\\(\\(?:\\w\\|[-_]\\)+\\):[      ]*"))
                     (not (and (equal marker "+")
                               (org-match-line
                                "[ \t]*\\(|[-+]+|?\\|\\+[-+]+\\+\\)[ \t]*$")))
                     (not (and (equal marker "*")
                               (save-excursion
                                 (forward-char)
                                 (skip-chars-backward "*")
                                 (looking-at-p org-outline-regexp-bol))))
                     (looking-at (if verbatim? org-verbatim-re org-emph-re))
                     (not (string-match-p org-element-paragraph-separate
                                          (match-string 2)))
                     (not (and (save-match-data (org-match-line "[ \t]*|"))
                               (string-match-p "|" (match-string 4))))))
              (pcase-let ((`(,_ ,face ,_) (assoc marker org-emphasis-alist))
                          (m (if org-hide-emphasis-markers 4 2)))
                (font-lock-prepend-text-property
                 (match-beginning m) (match-end m) 'face face)
                (when verbatim?
                  (org-remove-flyspell-overlays-in
                   (match-beginning 0) (match-end 0))
                  (when (and (org-fold-core-folding-spec-p 'org-link)
                             (org-fold-core-folding-spec-p 'org-link-description))
                    (org-fold-region (match-beginning 0) (match-end 0) nil 'org-link)
                    (org-fold-region (match-beginning 0) (match-end 0) nil 'org-link-description))
                  (remove-text-properties (match-beginning 2) (match-end 2)
                                          '(display t invisible t intangible t)))
                (add-text-properties (match-beginning 2) (match-end 2)
                                     '(font-lock-multiline t org-emphasis t))
                (when (and org-hide-emphasis-markers
                           (not (org-at-comment-p)))
                  (add-text-properties (match-end 4) (match-beginning 5)
                                       '(invisible t))
                  (add-text-properties (match-beginning 3) (match-end 3)
                                       '(invisible t)))
                (throw :exit t))))))))

  (advice-add #'org-do-emphasis-faces :override #'eli/org-do-emphasis-faces)

  (defun eli/org-element--parse-generic-emphasis (mark type)
    "Parse emphasis object at point, if any."
    (save-excursion
      (let ((origin (point)))
        (unless (bolp) (forward-char -1))
        (let ((opening-re
               (rx-to-string
                `(seq (or line-start (any space ?- ?\( ?' ?\" ?\{ nonascii))
                  ,mark
                  (not space)))))
          (when (looking-at opening-re)
            (goto-char (1+ origin))
            (let ((closing-re
                   (rx-to-string
                    `(seq
                      (not space)
                      (group ,mark)
                      (or (any space ?- ?. ?, ?\; ?: ?! ?? ?' ?\" ?\) ?\} ?\\ ?\[
                               nonascii)
                          line-end)))))
              (when (re-search-forward closing-re nil t)
                (let ((closing (match-end 1)))
                  (goto-char closing)
                  (let* ((post-blank (skip-chars-forward " \t"))
                         (contents-begin (1+ origin))
                         (contents-end (1- closing)))
                    (list type
                          (append
                           (list :begin origin
                                 :end (point)
                                 :post-blank post-blank)
                           (if (memq type '(code verbatim))
                               (list :value
                                     (and (memq type '(code verbatim))
                                          (buffer-substring
                                           contents-begin contents-end)))
                             (list :contents-begin contents-begin
                                   :contents-end contents-end)))))))))))))

  (advice-add #'org-element--parse-generic-emphasis :override #'eli/org-element--parse-generic-emphasis))
#+end_src

** Agenda
按 org-super-agenda 重构: 每个视图只用一个 agenda + 一个 todo block, 内部用 super-agenda group 分块。
#+begin_src emacs-lisp
(after! org
  (setq org-agenda-inhibit-startup t
        org-agenda-tags-column 'auto
        org-agenda-files '("~/org/inbox.org"
                           "~/org/notes/20260101T000010--habits-hub__habit_index.org"
                           "~/org/append-note.org"
                           "~/org/.calendar")
        org-default-notes-file "~/org/inbox.org")

  ;; Archive
  (setq org-archive-location
        (concat (expand-file-name ".archive/" org-directory)
                "%s_archive.org::"))

  ;; Time grid
  (setq org-agenda-use-time-grid t
        org-agenda-show-current-time-in-grid t
        org-agenda-time-grid
        '((daily today)
          (600 800 1000 1200 1400 1600 1800 2000 2200)
          " ┄┄┄┄┄ " "┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄")
        org-agenda-current-time-string
        "◀── now ─────────────────────")

  ;; Hide redundant tags in agenda
  (setq org-agenda-hide-tags-regexp "personal\\|habit\\|index")

  (setq org-stuck-projects '("" nil nil ""))

  (defun my/skip-habit ()
    "Skip entries with :STYLE: habit."
    (let ((subtree-end (save-excursion (org-end-of-subtree t))))
      (when (string= (org-entry-get nil "STYLE") "habit")
        subtree-end)))

  ;; ---- org-super-agenda ----
  (use-package! org-super-agenda
    :after org-agenda
    :config
    (org-super-agenda-mode 1)

    ;; super-agenda 在每个 group 标题行挂了一个独立 keymap,
    ;; 里面绑定了 agenda 的 H (holidays) / J (goto date) / j / k 等,
    ;; 与 evil 的 HJKL 移动冲突。
    ;;
    ;; 方案: 把冲突键从 header map 解绑, 让 evil motion 正常生效。
    ;; (如果想完全清空 header map, 用 (setq org-super-agenda-header-map (make-sparse-keymap)))
    (let ((map org-super-agenda-header-map))
      (define-key map (kbd "H") nil)
      (define-key map (kbd "J") nil)
      (define-key map (kbd "K") nil)
      (define-key map (kbd "L") nil)
      (define-key map (kbd "j") nil)
      (define-key map (kbd "k") nil)
      (define-key map (kbd "h") nil)
      (define-key map (kbd "l") nil)))

  ;; ---- super-agenda group 预设 ----
  ;; 抽出来命名, 方便在多个 custom command 里复用与对比

  (defvar my/super-agenda-groups-daily
    '((:name "⏰ 今日"
       :time-grid t
       :date today)
      (:name "🔥 过期"
       :scheduled past
       :deadline past)
      (:name "📅 即将到期 (3 天内)"
       :deadline future)
      (:name "⚡ Next Actions"
       :todo "NEXT")
      (:name "🚧 进行中"
       :todo "PROG")
      (:name "⏳ Waiting"
       :todo ("WAIT" "WAITING"))
      (:name "🌀 习惯"
       :habit t)
      (:discard (:anything t)))
    "Daily 视图分组: 以今天为中心。")

  (defvar my/super-agenda-groups-weekly
    '((:name "⏰ 今日"
       :time-grid t
       :date today)
      (:name "🔥 过期"
       :scheduled past
       :deadline past)
      (:name "📅 本周日程"
       :scheduled future
       :deadline future)
      (:name "🏢 工作"
       :and (:tag "work" :not (:habit t)))
      (:name "🏠 个人"
       :and (:tag "personal" :not (:habit t)))
      (:name "📚 学习"
       :and (:tag "learning" :not (:habit t)))
      (:name "📦 未分类"
       :and (:todo ("TODO" "NEXT" "PROG")
             :not (:tag ("work" "personal" "learning"))
             :not (:habit t)))
      (:name "🌀 习惯"
       :habit t)
      (:discard (:anything t)))
    "Weekly 视图分组: 日程在前, 按 tag 分类 backlog。")

  (defvar my/super-agenda-groups-gtd
    '((:name "⚡ Next Actions"
       :todo "NEXT")
      (:name "🚧 进行中"
       :todo "PROG")
      (:name "⏳ Waiting"
       :todo ("WAIT" "WAITING"))
      (:name "🧊 On Hold"
       :todo "HOLD")
      (:name "📋 Backlog · 工作"
       :and (:todo "TODO" :tag "work"))
      (:name "📋 Backlog · 个人"
       :and (:todo "TODO" :tag "personal"))
      (:name "📋 Backlog · 学习"
       :and (:todo "TODO" :tag "learning"))
      (:name "📋 Backlog · Emacs"
       :and (:todo "TODO" :tag "emacs"))
      (:name "📋 Backlog · 未分类"
       :and (:todo "TODO"
             :not (:tag ("work" "personal" "learning"))))
      (:discard (:anything t)))
    "GTD 周回顾视图分组。")

  ;; ---- 视图定义 ----
  ;; 三个视图都用 agenda + todo 两个 block:
  ;; - agenda block: 处理有时间戳的条目 (schedule/deadline/today/past)
  ;; - todo block:   处理无时间戳的纯任务
  ;; super-agenda 在每个 block 内部继续做分组。
  ;; 注意: 用反引号 ` 让 ,my/super-agenda-groups-* 在 setq 时展开为列表字面值。
  ;; 修改 defvar 后需要重新求值这个 setq (或重启 Emacs)。

  (setq org-agenda-custom-commands
        `(("d" "Daily · 今日聚焦"
           ((agenda ""
                    ((org-agenda-span 'day)
                     (org-agenda-start-day nil)
                     (org-deadline-warning-days 3)
                     (org-agenda-skip-scheduled-if-done t)
                     (org-agenda-skip-deadline-if-done t)
                     (org-agenda-overriding-header "")
                     (org-super-agenda-groups ',my/super-agenda-groups-daily)))
            (todo ""
                  ((org-agenda-overriding-header "── Inbox / 无时间任务 ──")
                   (org-agenda-skip-function 'my/skip-habit)
                   (org-super-agenda-groups
                    '((:name "⚡ Next Actions" :todo "NEXT")
                      (:name "🚧 进行中"      :todo "PROG")
                      (:name "⏳ Waiting"     :todo ("WAIT" "WAITING"))
                      (:discard (:anything t))))))))

          ("w" "Weekly · 周视图"
           ((agenda ""
                    ((org-agenda-span 'week)
                     (org-agenda-start-on-weekday 1)        ; 周一起
                     (org-deadline-warning-days 7)
                     (org-habit-show-habits nil)            ; 周视图不显示习惯网格
                     (org-agenda-overriding-header "")
                     (org-super-agenda-groups ',my/super-agenda-groups-weekly)))
            (todo ""
                  ((org-agenda-overriding-header "── Backlog · 按领域分类 ──")
                   (org-agenda-skip-function 'my/skip-habit)
                   (org-super-agenda-groups ',my/super-agenda-groups-weekly)))))

          ("g" "GTD · 周回顾"
           ((agenda ""
                    ((org-agenda-span 'day)
                     (org-agenda-start-day nil)
                     (org-agenda-overriding-header "── 今日日程 ──")
                     (org-super-agenda-groups
                      '((:name "⏰ 今日" :time-grid t :date today)
                        (:name "🔥 过期" :scheduled past :deadline past)
                        (:discard (:anything t))))))
            (todo ""
                  ((org-agenda-overriding-header "── 所有未完成任务 ──")
                   (org-agenda-skip-function 'my/skip-habit)
                   (org-super-agenda-groups ',my/super-agenda-groups-gtd)))))

          ;; 全局搜索入口, 不分组方便临时翻
          ("A" "All TODOs (no grouping)"
           ((alltodo ""
                     ((org-agenda-overriding-header "All Open TODOs")
                      (org-agenda-skip-function 'my/skip-habit))))))))
#+end_src

** Capture helpers
#+begin_src emacs-lisp
(after! org
  ;; ---- Append Note helper ----
  (defun my/append-note-goto-bottom ()
    "Move point to end of append-note.org with date separator."
    (let ((today-sep (format-time-string "-- %Y-%m-%d --")))
      (goto-char (point-max))
      (unless (save-excursion
                (goto-char (point-min))
                (search-forward today-sep nil t))
        (unless (bolp) (insert "\n"))
        (insert "\n" today-sep "\n")))
    (goto-char (point-max)))

  (defun my/append-note-open-at-end-h ()
    "Always place point at the end when visiting append-note.org."
    (when (and buffer-file-name
               (file-equal-p buffer-file-name
                             (expand-file-name "~/org/append-note.org")))
      (goto-char (point-max))))

  (add-hook 'find-file-hook #'my/append-note-open-at-end-h t)

  ;; ---- Habit capture helper ----
  (defun my/org-capture-habit ()
    "Generate a capture template for a habit."
    (let* ((name (read-string "Habit 名称: "))
           (raw  (read-string "提醒时间 (HH:MM): "))
           (repeat (completing-read "重复周期: "
                                    '(".+1d  — 每天（从完成日起）"
                                      ".+2d  — 每2天"
                                      ".+1w  — 每周"
                                      ".+2w  — 每2周"
                                      ".+1m  — 每月"
                                      "++1d  — 每天（固定日期）"
                                      "++1w  — 每周（固定星期）"
                                      ".+1d/2d — 每天，最多隔2天"
                                      ".+1d/3d — 每天，最多隔3天")
                                    nil t))
           (repeat-val (car (split-string repeat " ")))
           (parts (split-string raw ":"))
           (hour (string-to-number (nth 0 parts)))
           (min  (string-to-number (nth 1 parts)))
           (time (format "%02d:%02d" hour min))
           (today (format-time-string "%Y-%m-%d %a"))
           (end-min (+ min 5))
           (end-hour (+ hour (/ end-min 60)))
           (end-time (format "%02d:%02d" end-hour (% end-min 60))))
      (format "* TODO %s\nSCHEDULED: <%s %s %s>\n:PROPERTIES:\n:STYLE:    habit\n:calendar-id: yuanxiang424@gmail.com\n:END:\n:org-gcal:\n<%s %s-%s>\n:END:\n"
              name today time repeat-val today time end-time))))
#+end_src

** Capture target helpers (web article / protocol note)
#+begin_src emacs-lisp
(defun my/capture-web-article-target ()
  "Target function for org-capture: reference note from clipboard URL."
  (let* ((url (string-trim (current-kill 0 t)))
         (title (or (ignore-errors
                      (with-temp-buffer
                        (url-insert-file-contents url)
                        (goto-char (point-min))
                        (when (re-search-forward "<title>\\([^<]*\\)</title>" nil t)
                          (string-trim (match-string 1)))))
                    (read-string "Title: ")))
         (id (format-time-string "%Y%m%dT%H%M%S"))
         (slug (replace-regexp-in-string "[^a-zA-Z0-9一-鿿]+" "-"
                                         (downcase (string-trim title)) t t))
         (slug (replace-regexp-in-string "^-\\|-$" "" slug))
         (file (expand-file-name (format "notes/%s--%s__read.org" id slug) org-directory)))
    (set-buffer (org-capture-target-buffer file))
    (when (= (buffer-size) 0)
      (insert (format "#+title: %s\n#+date: [%s]\n#+filetags: :read:\n#+identifier: %s\n\n* Source\n%s\n\n* Summary\n\n* My Notes\n"
                      title (format-time-string "%Y-%m-%d %a") id url)))
    (goto-char (point-max))
    (or (re-search-backward "^\\* My Notes" nil t) (goto-char (point-max)))
    (forward-line 1)))

(defun my/protocol-note-target ()
  "Target for org-capture 'pn': reference note from org-protocol."
  (let* ((url   (or (plist-get org-store-link-plist :link)
                    (plist-get org-store-link-plist :url) ""))
         (title (or (plist-get org-store-link-plist :description)
                    (read-string "Title: ")))
         (slug  (replace-regexp-in-string
                 "^-\\|-$" ""
                 (replace-regexp-in-string
                  "[^a-zA-Z0-9一-鿿]+" "-"
                  (downcase (string-trim title)) t t)))
         (id    (format-time-string "%Y%m%dT%H%M%S"))
         (file  (expand-file-name (format "notes/%s--%s__read.org" id slug) org-directory)))
    (set-buffer (org-capture-target-buffer file))
    (when (= (buffer-size) 0)
      (insert (format "#+title: %s\n#+date: [%s]\n#+filetags: :read:\n#+identifier: %s\n\n* Source\n%s\n\n* Summary\n\n* My Notes\n"
                      title (format-time-string "%Y-%m-%d %a") id url)))
    (goto-char (point-max))
    (or (re-search-backward "^\\* My Notes" nil t) (goto-char (point-max)))
    (forward-line 1)))
#+end_src

** Capture templates
#+begin_src emacs-lisp
(after! org
  (setq org-capture-templates
        '(("a" "Append Note" plain
           (file+function "~/org/append-note.org" my/append-note-goto-bottom)
           "- %?"
           :empty-lines 1 :jump-to-captured t)
          ("i" "Inbox" entry (file "~/org/inbox.org")
           "* %?\n:PROPERTIES:\n:CREATED: %U\n:END:\n" :empty-lines 1)
          ("n" "Note" entry (file "~/org/inbox.org")
           "* %^{标题}  %^g\n:PROPERTIES:\n:CREATED: %U\n:END:\n\n%?"
           :empty-lines 1 :jump-to-captured t)
          ("t" "Task" entry (file "~/org/inbox.org")
           "* TODO %?\n:PROPERTIES:\n:CREATED: %U\n:END:\n" :empty-lines 1)
          ("j" "Journal" entry
           (file my/journal-capture-target)
           "* %<%H:%M>\n%?"
           :empty-lines 1 :jump-to-captured t)
          ("r" "r · 稍后读 [inbox]" entry (file "~/org/inbox.org")
           "* TODO [[%^{URL}][%^{Title}]]\n:PROPERTIES:\n:CREATED: %U\n:END:\n%?" :empty-lines 1)
          ("m" "Media")
          ("mm" "电影 · 想看" entry
           (file+olp "~/org/notes/20260101T000010--media-collection__index.org" "影视动漫" "电影" "想看")
           "**** TODO %^{片名}\n:PROPERTIES:\n:添加时间: %U\n:END:\n%?"
           :empty-lines 1 :jump-to-captured t)
          ("mM" "电影 · 看完" entry
           (file+olp "~/org/notes/20260101T000010--media-collection__index.org" "影视动漫" "电影" "看完")
           "**** DONE %^{片名}\n:PROPERTIES:\n:完成日期: %<[%Y-%m-%d]>\n:END:\n%?"
           :empty-lines 1 :jump-to-captured t)
          ("mt" "电视剧 · 想看" entry
           (file+olp "~/org/notes/20260101T000010--media-collection__index.org" "影视动漫" "电视剧" "想看")
           "**** TODO %^{片名}\n:PROPERTIES:\n:添加时间: %U\n:END:\n%?"
           :empty-lines 1 :jump-to-captured t)
          ("mT" "电视剧 · 看完" entry
           (file+olp "~/org/notes/20260101T000010--media-collection__index.org" "影视动漫" "电视剧" "看完")
           "**** DONE %^{片名}\n:PROPERTIES:\n:完成日期: %<[%Y-%m-%d]>\n:END:\n%?"
           :empty-lines 1 :jump-to-captured t)
          ("ma" "动漫 · 想看" entry
           (file+olp "~/org/notes/20260101T000010--media-collection__index.org" "影视动漫" "动漫" "想看")
           "**** TODO %^{片名}\n:PROPERTIES:\n:添加时间: %U\n:END:\n%?"
           :empty-lines 1 :jump-to-captured t)
          ("mA" "动漫 · 看完" entry
           (file+olp "~/org/notes/20260101T000010--media-collection__index.org" "影视动漫" "动漫" "看完")
           "**** DONE %^{片名}\n:PROPERTIES:\n:完成日期: %<[%Y-%m-%d]>\n:END:\n%?"
           :empty-lines 1 :jump-to-captured t)
          ("b" "Books")
          ("bb" "书 · 待阅读" entry
           (file+olp "~/org/notes/20260101T000010--reading-list__read_index.org" "待阅读")
           "*** TODO %^{书名}\n:PROPERTIES:\n:作者: %^{作者}\n:类型: %^{类型|小说|非虚构|理财|网文|漫画|其他}\n:来源: %^{来源|微信读书|豆瓣|Z-Library|实体书|其他}\n:添加时间: %U\n:END:\n%?"
           :empty-lines 1 :jump-to-captured t)
          ("bB" "书 · 阅读中" entry
           (file+olp "~/org/notes/20260101T000010--reading-list__read_index.org" "阅读中")
           "*** READING %^{书名}\n:PROPERTIES:\n:作者: %^{作者}\n:类型: %^{类型|小说|非虚构|理财|网文|漫画|其他}\n:来源: %^{来源|微信读书|豆瓣|Z-Library|实体书|其他}\n:添加时间: %U\n:END:\n%?"
           :empty-lines 1 :jump-to-captured t)
          ("w" "w · 精读笔记 [ref/]" plain (function my/capture-web-article-target)
           "%?"
           :empty-lines 1 :jump-to-captured t)
          ("h" "Habit" entry (file "~/org/notes/20260101T000010--habits-hub__habit_index.org")
           (function my/org-capture-habit)
           :empty-lines 1)
          ("pl" "Protocol: Read later" entry (file "~/org/inbox.org")
           "* TODO %:annotation\n:PROPERTIES:\n:CREATED: %U\n:END:\n%i\n"
           :immediate-finish t :jump-to-captured t)
          ("pn" "Protocol: Note → references/" plain
           (function my/protocol-note-target)
           "#+begin_quote\n%i\n#+end_quote\n%?"
           :jump-to-captured t)
          ;; 投资交易暂存：信息不完整时先进 inbox；正式入账用 `SPC n i b/s/p/B` 写入 Beancount。
          ("$" "Investment · 待补 Beancount 交易" entry
           (file+olp+datetree "~/org/inbox.org" "Investment")
           "* TODO 补 Beancount 交易：%^{动作|买入|卖出|分红|手续费} %^{标的/代码}\n:PROPERTIES:\n:CREATED: %U\n:交易日期: %^{交易日期}\n:成交价格: %^{成交价格}\n:成交份额: %^{成交份额}\n:成交金额: %^{成交金额}\n:手续费: %^{手续费|0}\n:END:\n%?"
           :empty-lines 1)
          ;; 银行理财赎回：SPC n i r
          ;; 个人养老金定投买入：SPC n i y；红利再投：SPC n i R/Y
          ;; 招行现金流入/流出：SPC n i C
          ;; 招行现金转入股票账户：SPC n i t
          ("I" "Investment · 快捷操作")
          ("Ir" "招行理财赎回" plain
           (function my/beancount-record-bank-redeem)
           "" :immediate-finish t)
          ("Iy" "养老金基金定投" plain
           (function my/beancount-record-pension-buy)
           "" :immediate-finish t)
          ("IR" "养老金红利再投" plain
           (function my/beancount-record-pension-reinvest)
           "" :immediate-finish t)
          ("Ic" "招行现金流入/流出" plain
           (function my/beancount-record-cmb-cashflow)
           "" :immediate-finish t)
          ("It" "招行现金转入股票账户" plain
           (function my/beancount-record-broker-transfer)
           "" :immediate-finish t))))
#+end_src

** Refile & Tags
#+begin_src emacs-lisp
(after! org
  (defun my/org-top-level-org-files (dir)
    "Return top-level non-hidden .org files in DIR."
    (let ((dir (expand-file-name dir))
          result)
      (dolist (path (directory-files dir t "^[^.].*\\.org$") (nreverse result))
        (when (file-regular-p path)
          (push path result)))))

  (defun my/org-notes-files () (my/org-top-level-org-files "~/org/notes/"))

  (setq org-refile-targets
        '(("~/org/inbox.org" :maxlevel . 1)
          ("~/org/append-note.org" :maxlevel . 1)
          (my/org-notes-files :maxlevel . 2)))
  (setq org-refile-use-outline-path 'file
        org-outline-path-complete-in-steps nil
        org-refile-allow-creating-parent-nodes 'confirm
        org-refile-use-cache nil)

  ;; ---- Tags ----
  (setq org-tag-alist '((:startgroup)
                        ("work" . ?w) ("personal" . ?p) ("learning" . ?l)
                        (:endgroup)
                        ("emacs" . ?e) ("ai" . ?a) ("tools" . ?t))))
#+end_src

** Babel: Image dir & Bash on Windows
#+begin_src emacs-lisp
(after! org
  (defun my/org-babel-image-dir ()
    (when buffer-file-name
      (let ((dir (expand-file-name ".images/" (file-name-directory buffer-file-name))))
        (make-directory dir t)
        dir)))

  (advice-add 'org-babel-temp-file :around
              (lambda (orig-fn prefix &optional suffix)
                (let ((dir (my/org-babel-image-dir)))
                  (if (and dir suffix (string-match-p "\\.\\(png\\|svg\\|pdf\\|jpg\\)$" suffix))
                      (let ((temporary-file-directory dir))
                        (funcall orig-fn prefix suffix))
                    (funcall orig-fn prefix suffix))))))

;; ---- Force org-babel shell blocks to use Git bash on Windows ----
(defvar my/org-babel-bash nil)
(setq my/org-babel-bash "C:/PROGRA~1/Git/usr/bin/bash.exe")
(after! ob-shell
  (define-advice org-babel-execute:shell
      (:around (orig body params) use-bash-on-windows)
    (if (and (eq system-type 'windows-nt)
             (file-exists-p my/org-babel-bash))
        (let ((shell-file-name my/org-babel-bash)
              (shell-command-switch "-c")
              (explicit-shell-file-name my/org-babel-bash))
          (funcall orig body params))
      (funcall orig body params)))
  (define-advice org-babel-execute:bash
      (:around (orig body params) use-bash-on-windows)
    (if (and (eq system-type 'windows-nt)
             (file-exists-p my/org-babel-bash))
        (let ((shell-file-name my/org-babel-bash)
              (shell-command-switch "-c")
              (explicit-shell-file-name my/org-babel-bash))
          (funcall orig body params))
      (funcall orig body params))))
#+end_src

** Media library auto-rebucket
#+begin_src emacs-lisp
(after! org
  (defun my/media-org-file-p ()
    "Return non-nil when visiting the media library file."
    (and (buffer-file-name)
         (file-equal-p (expand-file-name "~/org/notes/20260101T000010--media-collection__index.org")
                       (expand-file-name (buffer-file-name)))))

  (defun my/media-org-target-section-for-state (state)
    "Map TODO STATE to a media library section name."
    (pcase state
      ((or "TODO" "NEXT") "想看")
      ("PROG" "在看")
      ("DONE" "看完")
      ("FAIL" "已放弃")
      (_ nil)))

  (defun my/media-org-current-entry-context ()
    "Return plist for the current media entry, or nil when not applicable."
    (save-excursion
      (org-back-to-heading t)
      (when (= (org-outline-level) 4)
        (let (category section)
          (save-excursion
            (while (org-up-heading-safe)
              (pcase (org-outline-level)
                (3 (setq section (org-get-heading t t t t)))
                (2 (setq category (org-get-heading t t t t))))))
          (when (and (member category '("电影" "电视剧" "动漫"))
                     (member section '("想看" "在看" "看完" "已放弃")))
            (list :category category :section section))))))

  (defun my/media-org-find-section-position (category section)
    "Return buffer position of CATEGORY -> SECTION in media.org."
    (save-excursion
      (save-restriction
        (goto-char (point-min))
        (when (re-search-forward "^\\* 影视动漫$" nil t)
          (org-narrow-to-subtree)
          (goto-char (point-min))
          (when (re-search-forward (format "^\\*\\* %s$" (regexp-quote category)) nil t)
            (org-narrow-to-subtree)
            (goto-char (point-min))
            (when (re-search-forward (format "^\\*\\*\\* %s$" (regexp-quote section)) nil t)
              (line-beginning-position)))))))

  (defun my/media-org-rebucket-current-entry ()
    "Move the current media entry to the section implied by its TODO state."
    (interactive)
    (when-let* ((context (and (my/media-org-file-p)
                              (my/media-org-current-entry-context)))
                (target-section (my/media-org-target-section-for-state org-state))
                (category (plist-get context :category))
                (current-section (plist-get context :section)))
      (unless (equal current-section target-section)
        (let ((level (org-outline-level)))
          (org-cut-subtree)
          (when-let ((target-pos (my/media-org-find-section-position category target-section)))
            (goto-char target-pos)
            (org-end-of-subtree t t)
            (unless (bolp)
              (insert "\n"))
            (org-paste-subtree level))))))

  (add-hook 'org-after-todo-state-change-hook #'my/media-org-rebucket-current-entry))
#+end_src

** Books library auto-rebucket
#+begin_src emacs-lisp
(after! org
  (defun my/books-org-file-p ()
    "Return non-nil when visiting the books collection file."
    (and (buffer-file-name)
         (file-equal-p (expand-file-name "~/org/notes/20260101T000010--reading-list__read_index.org")
                       (expand-file-name (buffer-file-name)))))

  (defun my/books-org-target-section-for-state (state)
    "Map TODO STATE to a books collection section name."
    (pcase state
      ((or "TODO" "NEXT" "WAITING") "待阅读")
      ("READING" "阅读中")
      ("DONE" "已读完")
      ((or "DROPPED" "HOLD" "CANCELLED") "已放弃")
      (_ nil)))

  (defun my/books-org-current-entry-context ()
    "Return plist for the current books entry, or nil when not applicable."
    (save-excursion
      (org-back-to-heading t)
      (when (= (org-outline-level) 2)
        (let (section)
          (save-excursion
            (when (org-up-heading-safe)
              (setq section (org-get-heading t t t t))))
          (when (member section '("待阅读" "阅读中" "已读完" "已放弃"))
            (list :section section))))))

  (defun my/books-org-find-section-position (section)
    "Return buffer position of SECTION in books.org."
    (save-excursion
      (save-restriction
        (goto-char (point-min))
        (when (re-search-forward (format "^\\* %s$" (regexp-quote section)) nil t)
          (line-beginning-position)))))

  (defun my/books-org-rebucket-current-entry ()
    "Move the current books entry to the section implied by its TODO state."
    (interactive)
    (when-let* ((context (and (my/books-org-file-p)
                              (my/books-org-current-entry-context)))
                (target-section (my/books-org-target-section-for-state org-state))
                (current-section (plist-get context :section)))
      (unless (equal current-section target-section)
        (let ((level (org-outline-level)))
          (org-cut-subtree)
          (when-let ((target-pos (my/books-org-find-section-position target-section)))
            (goto-char target-pos)
            (org-end-of-subtree t t)
            (unless (bolp)
              (insert "\n"))
            (org-paste-subtree level))))))

  (add-hook 'org-after-todo-state-change-hook #'my/books-org-rebucket-current-entry))
#+end_src

** Archive done tasks
#+begin_src emacs-lisp
(after! org
  (defun my/org-entry-done-or-cancelled-p ()
    "Return non-nil when the current Org heading is DONE or CANCELLED."
    (member (org-get-todo-state) '("DONE" "CANCELLED")))

  (defun my/org-entry-has-done-or-cancelled-parent-p ()
    "Return non-nil when an ancestor heading is DONE or CANCELLED."
    (save-excursion
      (let (found)
        (while (and (not found) (org-up-heading-safe))
          (when (my/org-entry-done-or-cancelled-p)
            (setq found t)))
        found)))

  (defun my/org-archive-target-file ()
    "Return the expanded archive file path for the current Org buffer."
    (let* ((fname (file-name-nondirectory (buffer-file-name)))
           (location (replace-regexp-in-string "%s" fname org-archive-location t t)))
      (expand-file-name (car (split-string location "::")))))

  (defun my/org-archive-done-tasks ()
    "Archive DONE/CANCELLED tasks in the current file."
    (interactive)
    (unless (buffer-file-name)
      (user-error "Not visiting a file; open an Org file first"))
    (let (items)
      (org-with-wide-buffer
        (org-map-entries
         (lambda ()
           (unless (my/org-entry-has-done-or-cancelled-parent-p)
             (let ((beg (point))
                   (end (save-excursion (org-end-of-subtree t t))))
               (push (list :beg beg
                           :end end
                           :text (buffer-substring-no-properties beg end))
                     items))))
         "/DONE|FAIL" 'file))
      (setq items
            (sort items
                  (lambda (a b)
                    (> (plist-get a :beg) (plist-get b :beg)))))
      (let ((count (length items))
            (archive-file (my/org-archive-target-file)))
        (when items
          (make-directory (file-name-directory archive-file) t)
          (when (fboundp 'org-remove-inline-images)
            (org-remove-inline-images))
          (with-temp-buffer
            (when (file-exists-p archive-file)
              (insert-file-contents archive-file))
            (goto-char (point-max))
            (unless (bolp)
              (insert "\n"))
            (dolist (item (reverse items))
              (insert "\n" (plist-get item :text))
              (unless (bolp)
                (insert "\n")))
            (write-region (point-min) (point-max) archive-file nil 'silent))
          (save-excursion
            (dolist (item items)
              (delete-region (plist-get item :beg) (plist-get item :end))))
          (save-buffer))
        (message "Archived %d done/cancelled task(s) in %s"
                 count (file-name-nondirectory (buffer-file-name)))))))
#+end_src

** Org Sync
#+begin_src emacs-lisp
;; 1. 保存 org 文件后, 延迟触发一次同步 (防抖, 30s)
(defvar my/org-sync-timer nil)
(defun my/org-sync-debounced ()
  (when my/org-sync-timer (cancel-timer my/org-sync-timer))
  (setq my/org-sync-timer
        (run-with-idle-timer
         30 nil
         (lambda ()
           (start-process "org-sync" "*org-sync*"
                          "powershell" "-NoProfile" "-File"
                          (expand-file-name "~/org/.src/org-sync-runner.ps1"))))))

(add-hook 'after-save-hook
          (lambda ()
            (when (and buffer-file-name
                       (string-match-p "/org/" buffer-file-name)
                       (string-match-p "\\.org\\'" buffer-file-name))
              (my/org-sync-debounced))))

;; 2. 退出 Emacs 时后台触发一次同步（不阻塞退出）
(defun my/org-sync-on-exit ()
  (ignore-errors
    (let ((proc (start-process "org-sync-exit" nil
                               "powershell" "-NoProfile" "-File"
                               (expand-file-name "~/org/.src/org-sync-runner.ps1"))))
      (set-process-query-on-exit-flag proc nil))))
(add-hook 'kill-emacs-hook #'my/org-sync-on-exit)

;; 3. 手动命令
(defun my/org-sync-now ()
  (interactive)
  (async-shell-command
   (format "powershell -NoProfile -File %s"
           (expand-file-name "~/org/.src/org-sync-runner.ps1"))))
#+end_src

** Pcache compatibility (Emacs 31)
#+begin_src emacs-lisp
(after! pcache
  ;; pcache 0.5.1 uses obsolete `:slot' style in `oset', which causes
  ;; noisy warnings on Emacs 31 during kill-emacs hooks.
  (cl-defmethod pcache-save ((cache pcache-repository) &optional force)
    (let ((timestamp (oref cache timestamp))
          (delay (oref cache save-delay))
          (time (float-time (current-time))))
      (when (or force (> time (+ timestamp delay)))
        (oset cache timestamp time)
        (oset cache version
              (oref-default (eieio-object-class cache) version-constant))
        (eieio-persistent-save cache)))))
#+end_src
** latex
#+begin_src emacs-lisp
;; LaTeX previews
(use-package! org-fragtog
  :after org
  :hook
  (org-mode . org-fragtog-mode)
  :config
  (setq org-format-latex-header "\\documentclass{article}
  \\usepackage[usenames]{color}
  \\usepackage{xeCJK}
  \\setCJKmainfont{KingHwa_OldSong}
  \\usepackage{amsmath,amssymb}
  \\usepackage{unicode-math}
  \\setmathfont{Latin Modern Math}
  [PACKAGES]
  [DEFAULT-PACKAGES]
  \\pagestyle{empty}")
  (setq org-format-latex-options
        (plist-put org-format-latex-options :scale 2))
  (setq org-format-latex-options
        (plist-put org-format-latex-options :foreground 'default))
  (setq org-format-latex-options
        (plist-put org-format-latex-options :background 'default))
  ;; 用 xelatex + dvisvgm 生成 SVG，暗色主题下显示更好
  (setq org-preview-latex-default-process 'dvisvgm)
  ;; 覆盖 dvisvgm 流程：xelatex -no-pdf 生成 xdv，dvisvgm 转 svg
  (setf (alist-get 'dvisvgm org-preview-latex-process-alist)
        '(:programs ("xelatex" "dvisvgm")
          :description "xelatex > xdv > svg"
          :message "you need to install xelatex and dvisvgm."
          :image-input-type "xdv"
          :image-output-type "svg"
          ;; :image-size-adjust (1.7 . 1.5)
          :image-size-adjust (0.7 . 0.7)
          :latex-compiler ("xelatex -interaction nonstopmode -no-pdf -output-directory %o %f")
          :image-converter ("dvisvgm %f --no-fonts --exact-bbox --scale=%S -o %O")))
  ;; 打开 org 文件时自动渲染所有 LaTeX 公式
  (setq org-startup-with-latex-preview t))
#+end_src

* Org Clipboard & DnD
** Screenshot / rich paste helpers
#+begin_src emacs-lisp
(defun my/org-download-screenshot-command ()
  "Platform-appropriate screenshot command for org-download."
  (cond
   ((featurep :system 'windows)
    "powershell -Command \"Add-Type -AssemblyName System.Windows.Forms; $img = [System.Windows.Forms.Clipboard]::GetImage(); if ($img) { $img.Save('%s', [System.Drawing.Imaging.ImageFormat]::Png) } else { Write-Error 'No image in clipboard' }\"")
   ((featurep :system 'macos)
    "sh -c 'if command -v pngpaste >/dev/null 2>&1 && pngpaste \"$1\" >/dev/null 2>&1; then exit 0; else screencapture -i \"$1\"; fi' _ %s")
   (t
    "sh -c 'if command -v xclip >/dev/null 2>&1; then xclip -selection clipboard -t image/png -o > \"$1\" 2>/dev/null || true; fi; if [ ! -s \"$1\" ]; then if command -v wl-paste >/dev/null 2>&1; then wl-paste --no-newline --type image/png > \"$1\" 2>/dev/null || true; fi; fi; if [ ! -s \"$1\" ]; then if command -v maim >/dev/null 2>&1; then maim -s \"$1\"; elif command -v grim >/dev/null 2>&1 && command -v slurp >/dev/null 2>&1; then grim -g \"$(slurp)\" \"$1\"; fi; fi' _ %s")))

(defun my/org-download-denote-file-format (filename &optional keywords)
  "Rewrite FILENAME to denote style: <id>--<slug>__<keywords><ext>.
When KEYWORDS is non-nil, include them in the denote-style name."
  (let* ((ext  (or (file-name-extension filename t) ""))
         (base (or (file-name-sans-extension filename) ""))
         (id   (format-time-string denote-date-identifier-format)))
    (file-name-nondirectory
     (denote-format-file-name "./" id keywords base ext nil))))

(defun my/org-paste-rich ()
  "Paste rich text (HTML with images) from clipboard as Org content."
  (interactive)
  (unless buffer-file-name
    (user-error "Please save the current buffer first"))
  (pcase system-type
    ('windows-nt
     (let* ((img-dir (expand-file-name ".images" (file-name-directory buffer-file-name)))
            (script (expand-file-name "~/org/.src/clipboard-to-org.ps1"))
            (img-dir-win (replace-regexp-in-string "/" "\\\\" img-dir))
            (script-win (replace-regexp-in-string "/" "\\\\" script))
            (cmd (format "powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"%s\" -ImageDir \"%s\""
                         script-win img-dir-win))
            (out-file (string-trim (shell-command-to-string cmd))))
       (if (and (not (string-blank-p out-file))
                (file-exists-p out-file))
           (progn
             (insert-file-contents out-file)
             (delete-file out-file)
             (message "OK: rich text pasted"))
         (message "WARN: clipboard empty or conversion failed (out: %s)" out-file))))
    ('darwin
     (let* ((script (expand-file-name "~/org/.src/clipboard-to-org-macos.sh"))
            (img-dir (expand-file-name ".images" (file-name-directory buffer-file-name))))
       (cond
        ((file-exists-p script)
         (let ((out-file (string-trim (shell-command-to-string
                                       (format "sh %s %s" (shell-quote-argument script) (shell-quote-argument img-dir))))))
           (if (and (not (string-blank-p out-file))
                    (file-exists-p out-file))
               (progn
                 (insert-file-contents out-file)
                 (delete-file out-file)
                 (message "OK: rich text pasted"))
             (message "WARN: macOS clipboard conversion failed"))))
        ((executable-find "pbpaste")
         (let ((text (shell-command-to-string "pbpaste")))
           (if (string-blank-p text)
               (message "WARN: macOS clipboard empty")
             (insert text)
             (message "OK: plain text pasted (macOS fallback)"))))
        (t (message "WARN: pbpaste not found")))))
    (_ (message "WARN: rich text clipboard not implemented for this platform"))))
#+end_src

** org-download
#+begin_src emacs-lisp
(after! org
  (use-package! org-download
    :commands (org-download-clipboard org-download-screenshot org-download-yank org-download-delete)
    :config
    (add-hook 'org-mode-hook 'org-download-enable)
    (setq org-download-method 'directory
          org-download-image-dir (expand-file-name "data/" org-directory)
          org-download-heading-lvl nil
          org-download-timestamp ""
          org-download-image-org-width 800
          org-download-annotate-function (lambda (_link) "")
          org-download-screenshot-method (my/org-download-screenshot-command)
          org-download-file-format-function #'my/org-download-denote-file-format
          org-download-link-format-function
          (lambda (filename)
            (format "[[file:%s]]\n"
                    (org-link-escape
                     (file-relative-name filename default-directory)))))

    ;; Fix: org-download-dnd-fallback for Emacs 30+
    (when (fboundp 'dnd-handle-multiple-urls)
      (defun org-download-dnd-fallback (uri action)
        (let ((dnd-protocol-alist
               (rassq-delete-all
                'org-download-dnd
                (copy-alist dnd-protocol-alist))))
          (dnd-handle-multiple-urls
           (selected-window) (list uri) action))))

    ;; Fix: Full percent-decoding for non-ASCII filenames + Windows local paths.
    (defun org-download--fullname (link &optional ext)
      "Return the file name where LINK will be saved to.
[patched] Robust Windows path handling + full percent-decoding."
      (let ((filename
             (decode-coding-string
              (url-unhex-string
               (file-name-nondirectory
                (if (file-name-absolute-p link)
                    link
                  (or (car (url-path-and-query
                            (url-generic-parse-url link)))
                      link))))
              'utf-8))
            (dir (org-download--dir)))
        (when (string-match ".*?\\.\\(?:png\\|jpg\\)\\(.*\\)$" filename)
          (setq filename (replace-match "" nil nil filename 1)))
        (when ext
          (setq filename (concat (file-name-sans-extension filename) "." ext)))
        (abbreviate-file-name
         (expand-file-name
          (funcall org-download-file-format-function filename)
          dir))))

    ;; Fix: org-download-clipboard on Windows ignores user's screenshot-method.
    (when (featurep :system 'windows)
      (defadvice! my/org-download-clipboard-use-powershell (&optional basename)
        :override #'org-download-clipboard
        (org-id-get-create)
        (org-download-screenshot basename)))))
#+end_src

** Delete file link in Org
#+begin_src emacs-lisp
;; 用 org-element-context 解析链接, 支持 file:/attachment: 类型及带描述的 [[file:x.png][caption]]
(defun my/org-delete-file-link-at-point ()
  "Delete the file link at point and the underlying attached file."
  (interactive)
  (let* ((context (org-element-context))
         (type (org-element-property :type context)))
    (unless (member type '("file" "attachment"))
      (user-error "Not on a file or attachment link"))
    (let* ((path (org-element-property :path context))
           (unescaped (org-link-unescape path))
           (abspath (expand-file-name unescaped
                                      (if (equal type "attachment")
                                          (and (boundp 'org-attach-id-dir) org-attach-id-dir)
                                        default-directory)))
           (beg (org-element-property :begin context))
           (end (org-element-property :end context))
           (result (my/delete-file-link--try-delete abspath)))
      (when result
        (delete-region beg end)
        (when (and (looking-at "[ \t]*$") (not (bolp)))
          (delete-region (point) (line-end-position)))
        (message (if (eq result 'deleted)
                     "Deleted file and removed link"
                   "Removed link (file was already missing"))))))
#+end_src

** Rotate image file permanently
#+begin_src emacs-lisp
(defun my/org-rotate-image-file (&optional angle)
  "Rotate the image ATTACHMENT/FILE at point by ANGLE (default 90°) using ImageMagick.
With universal prefix (\\[universal-argument]), prompt for angle.
This physically modifies the file, so rotation survives cache/reboot."
  (interactive "P")
  (let* ((context (org-element-context))
         (type (org-element-property :type context))
         (path (when (member type '("file" "attachment"))
                 (org-element-property :path context)))
         (file (when path
                 (expand-file-name (org-link-unescape path)
                                   (if (equal type "attachment")
                                       (and (boundp 'org-attach-id-dir) org-attach-id-dir)
                                     default-directory))))
         (angle (cond ((numberp angle) angle)
                      ((not angle) 90)
                      (t (read-number "Rotation angle: " 90)))))
    (unless (and file (file-exists-p file) (file-regular-p file))
      (user-error "No image file at point"))
    (let* ((magick (or (executable-find "magick")
                        (let ((candidates '("C:/ProgramData/scoop/apps/imagemagick/current/magick.exe"
                                            "C:/PROGRA~3/scoop/apps/imagemagick/current/magick.exe")))
                          (catch 'found
                            (dolist (c candidates)
                              (when (file-executable-p c) (throw 'found c)))))))
           (cmd (format "\"%s\" mogrify -rotate %d \"%s\"" magick angle file)))
      (unless magick (user-error "ImageMagick not found"))
      (if (zerop (shell-command cmd))
          (progn
            (message "Rotated %s by %d°" (file-name-nondirectory file) angle)
            (org-redisplay-inline-images))
        (user-error "ImageMagick `mogrify` failed — check: magick mogrify -rotate 90 \"some.png\"")))))
#+end_src

** DnD: flat copy to data/ (Org)
#+begin_src emacs-lisp
(defun my/org-dnd-copy-to-data (uri _action)
  "Copy locally-dropped file URI into ~/org/data/ with a denote-style name.
Prompts for denote keywords interactively."
  (when (derived-mode-p 'org-mode)
    (let ((src (ignore-errors (dnd-get-local-file-name uri t))))
      (when (and src (file-regular-p src))
        (let* ((keywords (denote-keywords-prompt))
               (dest-dir  (expand-file-name "data/" org-directory))
               (dest-name (my/org-download-denote-file-format
                           (file-name-nondirectory src) keywords))
               (width     (or (bound-and-true-p org-download-image-org-width) 800))
               (dest      (expand-file-name dest-name dest-dir)))
          (make-directory dest-dir t)
          (copy-file src dest 1)
          (unless (zerop width)
            (insert (format "#+attr_org: :width %dpx\n" width)))
          (insert (format "[[file:%s]]"
                          (file-relative-name dest default-directory)))
          'copy)))))

(defun my/org-prepend-flat-dnd-handler ()
  "Make our flat-data DnD handler win over Org's builtin file: handlers."
  (setq-local dnd-protocol-alist
              (cons '("^file:" . my/org-dnd-copy-to-data)
                    dnd-protocol-alist)))
(add-hook 'org-mode-hook #'my/org-prepend-flat-dnd-handler)
#+end_src

** Delete file link in Markdown
#+begin_src emacs-lisp
;; 用 markdown-link-at-pos 解析, 同时处理 [file](pdf) 和 ![image](png)
(defun my/markdown-delete-file-link-at-point ()
  "Delete the file link at point and the underlying attached file."
  (interactive)
  (let ((info (markdown-link-at-pos (point))))
    (unless info (user-error "Not on a link"))
    (let* ((beg (nth 0 info)) (end (nth 1 info))
           (url (nth 3 info))
           (abspath (expand-file-name url default-directory))
           (result (my/delete-file-link--try-delete abspath)))
      (when result
        (delete-region beg end)
        (message (if (eq result 'deleted)
                     "Deleted file and removed link"
                   "Removed link (file was already missing"))))))
#+end_src

** DnD: Markdown
#+begin_src emacs-lisp
(defconst my/markdown-image-extensions
  '("png" "jpg" "jpeg" "gif" "svg" "webp" "bmp" "avif")
  "Extensions treated as images for markdown DnD insertion.")

(defun my/markdown-dnd-copy-to-data (uri _action)
  "Copy locally-dropped file URI into ~/org/data/ with denote-style name.
Prompts for denote keywords interactively."
  (when (derived-mode-p 'markdown-mode)
    (let ((src (ignore-errors (dnd-get-local-file-name uri t))))
      (when (and src (file-regular-p src))
        (let* ((keywords   (denote-keywords-prompt))
               (dest-dir   (expand-file-name "data/" org-directory))
               (orig-name  (file-name-nondirectory src))
               (dest-name  (my/org-download-denote-file-format orig-name keywords))
               (dest       (expand-file-name dest-name dest-dir))
               (ext        (downcase (or (file-name-extension orig-name) "")))
               (rel        (file-relative-name dest default-directory))
               (label      (file-name-base orig-name)))
          (make-directory dest-dir t)
          (copy-file src dest 1)
          (insert (if (member ext my/markdown-image-extensions)
                      (format "![%s](%s)" label rel)
                    (format "[%s](%s)" label rel)))
          'copy)))))

(defun my/markdown-prepend-flat-dnd-handler ()
  "Prepend our flat-data DnD handler buffer-locally in markdown-mode."
  (setq-local dnd-protocol-alist
              (cons '("^file:" . my/markdown-dnd-copy-to-data)
                    dnd-protocol-alist)))
(add-hook 'markdown-mode-hook #'my/markdown-prepend-flat-dnd-handler)
#+end_src

* Org-gcal
** plstore workaround
#+begin_src emacs-lisp
(after! plstore
  (advice-add 'plstore-save :around
              (lambda (orig-fun plstore)
                (let ((secret-alist (copy-tree (plstore--get-secret-alist plstore))))
                  (dolist (sec secret-alist)
                    (let ((pub (assoc (car sec) (plstore--get-alist plstore))))
                      (when pub (nconc pub (cdr sec)))))
                  (plstore--set-secret-alist plstore nil)
                  (unwind-protect
                      (funcall orig-fun plstore)
                    (plstore--set-secret-alist plstore secret-alist))))))
#+end_src

** Config & helpers
#+begin_src emacs-lisp
(use-package! org-gcal
  :commands (org-gcal-sync org-gcal-fetch org-gcal-delete-at-point org-gcal-post-at-point)
  :config
  (setq org-gcal-up-days 7
        org-gcal-down-days 60)

  (defvar my/org-gcal-default-calendar-id
    "f3f2ce4fb88adc5db8f25b71d3c75d20924a8c147a0feb34eafe477f173a860b@group.calendar.google.com")

  (defun my/org-gcal-drawer-timestamp ()
    "返回当前 entry 的 :org-gcal: drawer 里的时间戳。"
    (save-excursion
      (let ((end (save-excursion (outline-next-heading) (point))))
        (when (re-search-forward ":org-gcal:" end t)
          (let ((drawer-end (save-excursion
                              (re-search-forward ":END:" end t)
                              (point))))
            (let ((content (buffer-substring-no-properties (point) drawer-end)))
              (when (string-match "<[^>]+>" content)
                (match-string 0 content))))))))

  (defun my/org-gcal-set-drawer (timestamp)
    "把 TIMESTAMP 写入 :org-gcal: drawer。"
    (save-excursion
      (let* ((entry-start (point))
             (entry-end   (save-excursion (outline-next-heading) (point))))
        (goto-char entry-start)
        (if (re-search-forward "^:org-gcal:$" entry-end t)
            (let ((content-start (point)))
              (re-search-forward "^:END:$" entry-end t)
              (beginning-of-line)
              (delete-region content-start (point))
              (insert "\n" timestamp "\n"))
          (goto-char entry-start)
          (if (re-search-forward "^:END:$" entry-end t)
              (progn (end-of-line) (insert "\n:org-gcal:\n" timestamp "\n:END:"))
            (org-end-of-meta-data nil)
            (insert ":org-gcal:\n" timestamp "\n:END:\n"))))))

  (defun my/org-gcal-patch-status (calendar-id event-id gcal-status)
    "PATCH GCal event status."
    (require 'request)
    (let ((url (concat (org-gcal-events-url calendar-id)
                       "/" (url-hexify-string event-id)))
          (token (org-gcal--get-access-token calendar-id)))
      (request url
        :type "PATCH"
        :headers `(("Content-Type"  . "application/json")
                   ("Accept"        . "application/json")
                   ("Authorization" . ,(format "Bearer %s" token)))
        :data (json-encode `(("status" . ,gcal-status)))
        :parser 'org-gcal--json-read
        :success (cl-function
                  (lambda (&key _data &allow-other-keys)
                    (message "org-gcal: status → %s ✓ (%s)" gcal-status event-id)))
        :error (cl-function
                (lambda (&key error-thrown &allow-other-keys)
                  (message "org-gcal: PATCH status failed: %S" error-thrown))))))

  (defun my/org-gcal-todo-to-gcal-status (todo-state)
    (cond
     ((member todo-state '("DROPPED" "CANCELLED"))  "cancelled")
     ((member todo-state '("TODO" "NEXT" "WAITING" "HOLD" "DONE")) "confirmed")
     (t nil)))

  (defun my/org-gcal-auto-post ()
    "Auto push/update entries with timestamps to GCal."
    (interactive)
    (when (derived-mode-p 'org-mode)
      (org-save-outline-visibility t
        (org-map-entries
         (lambda ()
           (let* ((scheduled   (org-entry-get nil "SCHEDULED"))
                  (deadline    (org-entry-get nil "DEADLINE"))
                  (timestamp   (or scheduled deadline))
                  (has-id      (org-entry-get nil "entry-id"))
                  (calendar-id (or (org-entry-get nil "calendar-id")
                                   my/org-gcal-default-calendar-id))
                  (todo-state  (org-get-todo-state))
                  (gcal-status (my/org-gcal-todo-to-gcal-status todo-state))
                  (last-state  (org-entry-get nil "gcal-todo-state"))
                  (state-changed (and has-id gcal-status
                                      (not (equal last-state todo-state))))
                  (drawer-ts   (my/org-gcal-drawer-timestamp))
                  (ts-changed  (and timestamp has-id
                                    (or (not drawer-ts)
                                        (not (string= (string-trim timestamp)
                                                      (string-trim drawer-ts)))))))
             (when (and timestamp (or (not has-id) ts-changed))
               (my/org-gcal-set-drawer timestamp)
               (org-entry-put nil "calendar-id" calendar-id)
               (condition-case err
                   (org-gcal-post-at-point)
                 (error (message "org-gcal push failed: %s" err))))
             (when state-changed
               (let ((event-id (org-gcal--get-id (point))))
                 (when event-id
                   (org-entry-put nil "gcal-todo-state" todo-state)
                   (my/org-gcal-patch-status calendar-id event-id gcal-status))))))
         nil 'file))))

  ;; Dedup after fetch
  (defun my/org-gcal-dedup-after-fetch ()
    (let ((fetch-files (mapcar #'cdr org-gcal-fetch-file-alist))
          (known-ids (make-hash-table :test #'equal)))
      (dolist (file (org-agenda-files t))
        (unless (member (expand-file-name file) (mapcar #'expand-file-name fetch-files))
          (when (file-exists-p file)
            (with-temp-buffer
              (insert-file-contents file)
              (goto-char (point-min))
              (while (re-search-forward "^[ \t]*:entry-id:[ \t]+\\(.+\\)" nil t)
                (puthash (string-trim (match-string 1)) file known-ids))))))
      (dolist (fetch-file fetch-files)
        (let ((fpath (expand-file-name fetch-file)))
          (when (file-exists-p fpath)
            (with-current-buffer (find-file-noselect fpath)
              (org-with-wide-buffer
               (goto-char (point-min))
               (let ((kill-list nil))
                 (org-map-entries
                  (lambda ()
                    (let ((eid (org-entry-get nil "entry-id")))
                      (when (and eid (gethash eid known-ids))
                        (push (point) kill-list)))))
                 (when kill-list
                   (dolist (pos (sort kill-list #'>))
                     (goto-char pos)
                     (org-cut-subtree))
                   (save-buffer)
                   (message "org-gcal dedup: removed %d duplicate(s) from %s"
                            (length kill-list) fetch-file))))))))))

  (advice-add 'org-gcal-fetch :after
              (lambda (&rest _) (run-with-idle-timer 5 nil #'my/org-gcal-dedup-after-fetch)))

  ;; Periodic sync: idle timer 替代固定周期
  ;; 用户 idle 600s (10min) 后才触发, 避免后台静默偷网络/CPU
  ;; repeat 间隔 1800s, 但需要 idle 状态才会执行
  (run-with-idle-timer 600 t
                       (lambda ()
                         (when (not org-gcal--sync-lock)
                           (org-gcal-sync)
                           (my/org-gcal-auto-post)))))
#+end_src

* Knowledge Base
** Denote core
#+begin_src emacs-lisp
(use-package! denote
  :demand t
  :init
  (make-directory (expand-file-name "denote/" org-directory) t)
  :config
  (setq denote-directory (expand-file-name "notes/" org-directory)
        denote-file-type 'org
        denote-known-keywords  '(;; 工作/生活分界
                                 "work"
                                 ;; 领域
                                 "gamedev" "emacs" "ai" "pkm" "investment"
                                 "health" "habit" "read" "hire"
                                 ;; 跨域特殊标记
                                 "idea" "memo" "tool" "project" "index"
                                 ;; 形态标记
                                 "journal" "document")
        denote-infer-keywords nil
        denote-sort-keywords t
        denote-prompts '(title keywords)
        denote-date-prompt-use-org-read-date t
        denote-excluded-directories-regexp
        (rx (or (seq bos ".")
                (seq bos (or "data" "investment" "templates" "tmp") eos)))
        denote-rename-confirmations nil
        denote-dired-directories (list denote-directory)
        denote-dired-directories-include-subdirectories t)

  ;; Windows-safe replacement for `denote-dired-mode-in-directories'.
  (defun my/denote-dired-mode-maybe ()
    "Enable `denote-dired-mode' when current dired buffer is under a
denote-dired directory, matching case-insensitively."
    (when (derived-mode-p 'dired-mode)
      (let ((dir (file-name-as-directory (expand-file-name default-directory))))
        (when (seq-some
               (lambda (root)
                 (let ((root (file-name-as-directory (expand-file-name root))))
                   (or (string-equal-ignore-case dir root)
                       (and denote-dired-directories-include-subdirectories
                            (eq t (compare-strings
                                   root 0 nil
                                   dir  0 (length root)
                                   t))))))
               denote-dired-directories)
          (when (bound-and-true-p diredfl-mode)
            (diredfl-mode -1))
          (denote-dired-mode 1)
          (font-lock-flush)))))

  (add-hook 'after-change-major-mode-hook #'my/denote-dired-mode-maybe t)

  ;; Strip newlines from the complete filename.
  (advice-add 'denote-format-file-name :filter-return
              (lambda (filename) (replace-regexp-in-string "[\n\r]" "" filename))))
#+end_src

** Dirvish
#+begin_src emacs-lisp
(after! dirvish
  (setq dirvish-hide-details t)
  (map! :map dirvish-mode-map
        :n "/" #'dirvish-narrow))
#+end_src

** denote-org & denote-markdown
#+begin_src emacs-lisp
(use-package! denote-org
  :after (denote org))

(use-package! denote-markdown
  :after (denote markdown-mode)
  :commands (denote-markdown-convert-links-to-file-paths
             denote-markdown-convert-links-to-denote-type
             denote-markdown-convert-links-to-obsidian-type
             denote-markdown-convert-obsidian-links-to-denote-type))
#+end_src

** consult-notes
#+begin_src emacs-lisp
(use-package! consult-notes
  :after (consult denote)
  :commands (consult-notes consult-notes-search-in-all-notes)
  :config
  (setq consult-notes-file-dir-sources
        `(("Notes"   ?n ,(expand-file-name "notes/"   org-directory))))


  (setq consult-notes-org-headings-files
        (list (expand-file-name "inbox.org"       org-directory)
              (expand-file-name "append-note.org" org-directory)))
  (consult-notes-org-headings-mode)

  (when (locate-library "denote")
    (consult-notes-denote-mode))

  (setq consult-notes-denote-files-function
        (lambda () (denote-directory-files nil t t))))

(after! consult-notes
  (consult-customize consult-notes :sort nil)
  (setq consult-notes-denote-files-function
        (lambda ()
          (sort (denote-directory-files)
                (lambda (a b)
                  (time-less-p
                   (file-attribute-modification-time (file-attributes b))
                   (file-attribute-modification-time (file-attributes a)))))))
  ;; 文件名筛选不区分大小写
  (setq completion-ignore-case t)
  ;; 全文搜索不区分大小写 (ripgrep)
  (setq consult-ripgrep-args
        "rg --null --line-buffered --color=never --max-columns=1000 --path-separator / --no-heading --with-filename --line-number --search-zip --ignore-case"))
#+end_src

** org-transclusion (cross-note embedding)
#+begin_src emacs-lisp
;; org-transclusion: 把另一个 org 文件/章节实时嵌入当前 buffer
;; 用法: 插入 #+transclude: [[file:other.org::*某章节]]
;;      然后 M-x org-transclusion-add 展开
;; 适合: 把 denote 笔记里的某段话引用到日记 / 周报
(use-package! org-transclusion
  :after org
  :commands (org-transclusion-add
             org-transclusion-add-all
             org-transclusion-mode))
#+end_src

* Writing
** jinx (spell-check)
#+begin_src emacs-lisp
;; jinx: 基于 enchant 的高速增量拼写检查
;; - 比 flyspell 快一个数量级, 只检查可见区域
;; - Windows/MSYS2 依赖: pkg-config/pkgconf 能找到 enchant-2
;;   也就是 `pkg-config --exists enchant-2` 返回 0
;; - 首次使用: M-x jinx-languages 选择字典 (如 en_US)
;; - 在错词上 M-$ 显示候选
;; - 默认不在 org-mode 开启 (中英混排误报较多), 想开启在 hook 里加上
;; - Windows 上 jinx-mod.dll + enchant 已触发过 Emacs native crash,
;;   所以这里不在 Windows 自动启用。
(use-package! jinx
  :if (not (featurep :system 'windows))
  :hook ((markdown-mode . jinx-mode))
  :bind (("M-$"   . jinx-correct)
         ("C-M-$" . jinx-languages)))
#+end_src

** Focus mode toggle
#+begin_src emacs-lisp
;; 一键切换"专注写作"模式: 隐藏 modeline, 加宽 olivetti
;; (defvar my/focus-writing-active nil)

;; (defun my/focus-writing-toggle ()
;;   "Toggle distraction-free writing layout."
;;   (interactive)
;;   (if my/focus-writing-active
;;       (progn
;;         (when (fboundp 'hide-mode-line-mode) (hide-mode-line-mode -1))
;;         (when (bound-and-true-p olivetti-mode)
;;           (setq olivetti-body-width 92)
;;           (olivetti--set-margins))
;;         (setq my/focus-writing-active nil)
;;         (message "Focus mode: OFF"))
;;     (unless (bound-and-true-p olivetti-mode) (olivetti-mode 1))
;;     (setq olivetti-body-width 110)
;;     (when (fboundp 'olivetti--set-margins) (olivetti--set-margins))
;;     (when (fboundp 'hide-mode-line-mode) (hide-mode-line-mode 1))
;;     (setq my/focus-writing-active t)
;;     (message "Focus mode: ON")))
#+end_src

* Journal
** denote-journal
#+begin_src emacs-lisp
(use-package! denote-journal
  :after denote
  :hook (calendar-mode . denote-journal-calendar-mode)
  :init
  (with-eval-after-load 'calendar
    (define-key calendar-mode-map (kbd "f") #'denote-journal-calendar-find-file)
    (define-key calendar-mode-map (kbd "n") #'denote-journal-calendar-new-or-existing)
    (with-eval-after-load 'evil
      (evil-define-key '(normal motion emacs) calendar-mode-map
        (kbd "F") #'denote-journal-calendar-find-file
        (kbd "N") #'denote-journal-calendar-new-or-existing)))
  :config
  (setq denote-journal-directory (expand-file-name "journal/" org-directory)
        denote-journal-keyword "journal"
        denote-journal-title-format 'day-date-month-year))

(defun my/journal-capture-target ()
  "Return path to today's denote-journal entry for `org-capture'.
Before 03:00 falls back to the previous day."
  (let* ((now  (decode-time))
         (hour (nth 2 now))
         (time (if (< hour 3)
                   (time-subtract (current-time) (seconds-to-time 86400))
                 (current-time))))
    (denote-journal-path-to-new-or-existing-entry
     (format-time-string "%Y-%m-%d" time))))
#+end_src

** Weather (wttr.in) — async
#+begin_src emacs-lisp
(defvar my/weather-location "上海"
  "wttr.in 查询地点。")

(defun my/get-weather ()
  "同步从 wttr.in 获取天气 (手动调用 my/insert-weather 时用)。"
  (let ((coding-system-for-read 'utf-8))
    (string-trim
     (shell-command-to-string
      (format "curl -s --max-time 3 \"https://wttr.in/%s?format=3\""
              (url-hexify-string my/weather-location))))))

(defun my/insert-weather ()
  "在光标处插入当前天气 (同步, 可能卡 ≤3s)。"
  (interactive)
  (insert (my/get-weather)))

(defun my/denote-journal--insert-weather-async (target-file)
  "异步获取天气并写入 TARGET-FILE 顶部, 避免新建 journal 时卡顿。"
  (let ((buf (generate-new-buffer " *weather*")))
    (make-process
     :name "weather-fetch"
     :buffer buf
     :command (list "curl" "-s" "--max-time" "3"
                    (format "https://wttr.in/%s?format=3"
                            (url-hexify-string my/weather-location)))
     :sentinel
     (lambda (proc _event)
       (when (memq (process-status proc) '(exit signal))
         (let ((output (with-current-buffer buf
                         (string-trim (buffer-string)))))
           (kill-buffer buf)
           (when (and (file-exists-p target-file)
                      (not (string-empty-p output)))
             (with-current-buffer (find-file-noselect target-file)
               (save-excursion
                 (goto-char (point-min))
                 (unless (re-search-forward "^\\* 天气\\(?:[ \t].*\\)?$" nil t)
                   (goto-char (point-min))
                   (while (looking-at-p "^#\\+")
                     (forward-line 1))
                   (while (looking-at-p "^[ \t]*$")
                     (forward-line 1))
                   (insert "* 天气\n" output "\n\n")))
               (save-buffer)))))))))

(defun my/denote-journal-auto-weather ()
  "新建 journal 文件时异步插入顶部天气, 不阻塞 capture 流程。"
  (when (and (buffer-file-name)
             (string-match-p "__journal" (buffer-file-name)))
    (my/denote-journal--insert-weather-async (buffer-file-name))))

(add-hook 'denote-after-new-note-hook #'my/denote-journal-auto-weather)
#+end_src

* Calendar (cal-china-x)
#+begin_src emacs-lisp
(use-package! cal-china-x
  :after calendar
  :config
  (setq calendar-mark-holidays-flag t
        cal-china-x-important-holidays cal-china-x-chinese-holidays
        calendar-holidays
        (append cal-china-x-important-holidays
                cal-china-x-general-holidays))
  (setq org-agenda-include-diary nil))
#+end_src

* Reading
** PDF/EPUB org-file-apps
#+begin_src emacs-lisp
(after! org
  (defun my/org-open-pdf-with-pdf-tools (file _link)
    (require 'pdf-tools nil t)
    (require 'pdf-view nil t)
    (find-file file)
    (unless (derived-mode-p 'pdf-view-mode)
      (pdf-view-mode)))

  (defun my/org-open-epub-with-nov (file _link)
    (require 'nov nil t)
    (find-file file)
    (unless (derived-mode-p 'nov-mode)
      (nov-mode)))

  (let ((pdf-entry (assoc "\\.pdf\\'" org-file-apps)))
    (if pdf-entry
        (setcdr pdf-entry #'my/org-open-pdf-with-pdf-tools)
      (add-to-list 'org-file-apps '("\\.pdf\\'" . my/org-open-pdf-with-pdf-tools))))
  (let ((epub-entry (assoc "\\.epub\\'" org-file-apps)))
    (if epub-entry
        (setcdr epub-entry #'my/org-open-epub-with-nov)
      (add-to-list 'org-file-apps '("\\.epub\\'" . my/org-open-epub-with-nov)))))
#+end_src

** pdf-view
#+begin_src emacs-lisp
(after! pdf-view
  (setq-default pdf-view-display-size 'fit-page)
  (setq pdf-view-midnight-colors '("#ffffff" . "#1e1e2e"))
  (map! :map pdf-view-mode-map
        "C-c C-a h" #'pdf-annot-add-highlight-markup-annotation
        "C-c C-a t" #'pdf-annot-add-text-annotation
        "C-c C-a d" #'pdf-annot-add-strikeout-markup-annotation))
#+end_src

** nov.el
#+begin_src emacs-lisp
(use-package! nov
  :mode ("\\.epub\\'" . nov-mode)
  :config
  (when (featurep :system 'windows)
    (setq nov-unzip-program "C:/Program Files/Git/usr/bin/unzip.exe"))
  (setq nov-variable-pitch nil)

  (defun my/nov-setup ()
    (let* ((available (font-family-list))
           (font (or (cl-find-if (lambda (f) (member f available))
                                 '("Noto Sans SC" "Noto Serif SC"
                                   "Microsoft YaHei UI" "Microsoft YaHei"))
                     "Microsoft YaHei")))
      (face-remap-add-relative 'default :family font :height 140)
      (face-remap-add-relative 'variable-pitch :family font :height 140)
      (set-fontset-font t nil (font-spec :family font) nil 'prepend)
      (message "nov: using font \"%s\"" font))
    (visual-line-mode 1)
    (setq nov-text-width 80))
  (add-hook 'nov-mode-hook #'my/nov-setup)

  ;; Fix: nov-save-place 中文路径 utf-8
  (defun my/nov-save-place-a (orig-fn &rest args)
    (let ((coding-system-for-write 'utf-8))
      (apply orig-fn args)))
  (advice-add 'nov-save-place :around #'my/nov-save-place-a))
#+end_src

** org-noter
#+begin_src emacs-lisp
(use-package! org-noter
  :commands org-noter
  :config
  (setq org-noter-notes-search-path '("~/org/notes/")
        org-noter-default-notes-file-names '("notes.org")
        org-noter-auto-save-last-location t
        org-noter-notes-window-location 'horizontal-split
        org-noter-highlight-selected-text t)

  (defun my/org-noter-create-session-in-references (&optional arg document-file-name)
    "Create org-noter session, always placing notes in ~/org/notes/."
    (let* ((document-file-name (or (run-hook-with-args-until-success
                                    'org-noter-get-buffer-file-name-hook major-mode)
                                   document-file-name))
           (document-path (or document-file-name buffer-file-truename
                              (error "This buffer does not seem to be visiting any file")))
           (document-name (file-name-nondirectory document-path))
           (document-base (file-name-base document-name))
           (notes-dir (expand-file-name "~/org/notes/"))
           (notes-file (expand-file-name (concat document-base ".org") notes-dir)))
      (make-directory notes-dir t)
      (unless (file-exists-p notes-file)
        (with-temp-file notes-file
          (insert (format "#+title: %s\n#+filetags: :ref:\n#+created: %s\n\n* %s\n:PROPERTIES:\n:NOTER_DOCUMENT: %s\n:END:\n"
                          document-base
                          (format-time-string "[%Y-%m-%d %a]")
                          document-base
                          (expand-file-name document-path)))))
      (with-current-buffer (find-file-noselect notes-file)
        (goto-char (point-min))
        (re-search-forward (org-re-property org-noter-property-doc-file) nil t)
        (org-back-to-heading t)
        (org-noter))))

  (setq org-noter-create-session-from-document-hook
        '(my/org-noter-create-session-in-references)))
#+end_src

* Investment
** beancount
#+begin_src emacs-lisp
;; beancount: 复式记账 / 投资记录
;; - 安装 CLI:  pip install beancount fava
;; - 主账本放在 ~/org/investment/main.beancount
;; - 正式交易/现金流用下面的 `my/beancount-record-*` 命令直接入账；org-capture 只做信息不完整时的暂存。
;; - 通过 fava (浏览器 UI) 查看报表: fava ~/org/investment/main.beancount
(use-package! beancount
  :mode ("\\.beancount\\'" . beancount-mode)
  :config
  (setq beancount-electric-currency t
        beancount-use-ido nil))           ; 用 vertico 接管补全

(require 'subr-x)
(require 'cl-lib)

(defconst my/beancount-funds
  '(("159501" :name "嘉实纳斯达克100ETF(QDII)" :commodity "ETF159501"
     :account "Assets:Investment:Nasdaq100:ETF159501" :category "纳指100")
    ("159696" :name "易方达纳斯达克100ETF(QDII)" :commodity "ETF159696"
     :account "Assets:Investment:Nasdaq100:ETF159696" :category "纳指100")
    ("513100" :name "国泰纳斯达克100ETF(QDII)" :commodity "ETF513100"
     :account "Assets:Investment:Nasdaq100:ETF513100" :category "纳指100")
    ("513870" :name "富国纳斯达克100ETF(QDII)" :commodity "ETF513870"
     :account "Assets:Investment:Nasdaq100:ETF513870" :category "纳指100")
    ("511880" :name "银华日利ETF" :commodity "ETF511880"
     :account "Assets:Investment:MoneyMarket:ETF511880" :category "货币基金" :fee-free t))
  "投资账本中的基金/ETF 映射。")

(defconst my/beancount-bank-wealth
  '(("DYB" :name "招行多月宝" :commodity "WMDYB"
     :account "Assets:Investment:BankWealth:DYB" :category "银行理财" :fee-free t)
    ("DQB" :name "招行定期宝" :commodity "WMDQB"
     :account "Assets:Investment:BankWealth:DQB" :category "银行理财" :fee-free t))
  "银行理财映射。")

(defconst my/beancount-pension-funds
  '(("022907" :name "易方达创业板ETF联接Y" :commodity "P022907"
     :account "Assets:Investment:Pension:Fund022907" :category "个人养老金" :fee-free t)
    ("022918" :name "南方中证A500ETF联接Y" :commodity "P022918"
     :account "Assets:Investment:Pension:Fund022918" :category "个人养老金" :fee-free t)
    ("022945" :name "华夏上证科创板50成分ETF联接Y" :commodity "P022945"
     :account "Assets:Investment:Pension:Fund022945" :category "个人养老金" :fee-free t)
    ("022980" :name "天弘中证红利低波动100ETF联接Y" :commodity "P022980"
     :account "Assets:Investment:Pension:Fund022980" :category "个人养老金" :fee-free t))
  "个人养老金基金映射。")

(defun my/beancount--bank-wealth-candidates ()
  "Return completion candidates for bank wealth products."
  (mapcar (lambda (cell)
            (let* ((code (car cell))
                   (fund (cdr cell))
                   (name (plist-get fund :name)))
              (cons (format "%s · %s" code name) code)))
          my/beancount-bank-wealth))

(defun my/beancount--pension-candidates ()
  "Return completion candidates for pension funds."
  (mapcar (lambda (cell)
            (let* ((code (car cell))
                   (fund (cdr cell))
                   (name (plist-get fund :name)))
              (cons (format "%s · %s" code name) code)))
          my/beancount-pension-funds))

(defun my/beancount--bank-wealth-fund (code)
  "Return plist for bank wealth CODE."
  (or (cdr (assoc code my/beancount-bank-wealth))
      (user-error "Unknown bank wealth product: %s" code)))

(defun my/beancount--pension-fund (code)
  "Return plist for pension fund CODE."
  (or (cdr (assoc code my/beancount-pension-funds))
      (user-error "Unknown pension fund: %s" code)))

(defun my/beancount--append-bank-wealth-entry (entry &optional date code)
  "Append ENTRY to main ledger for bank wealth products."
  (find-file (my/beancount-main-file))
  (goto-char (point-max))
  (unless (bolp) (insert "\n"))
  (insert "\n" entry)
  (save-buffer)
  (my/beancount-check-main))

(defun my/beancount-record-bank-redeem ()
  "快速记录招行理财（多月宝/定期宝）赎回。"
  (interactive)
  (let* ((date (my/beancount--read-date))
         (code (let* ((candidates (my/beancount--bank-wealth-candidates))
                     (choice (completing-read "理财产品: " candidates nil t)))
                 (cdr (assoc choice candidates))))
         (fund (my/beancount--bank-wealth-fund code))
         (name (plist-get fund :name))
         (commodity (plist-get fund :commodity))
         (account (plist-get fund :account))
         (amount (my/beancount--read-number-string "赎回金额: "))
         (shares (my/beancount--read-number-string "赎回份额: "))
         (price-input (read-string "赎回净值(可空，自动=金额/份额): "))
         (price (if (string-empty-p price-input)
                    (my/beancount--normalize-number
                     (format "%.4f" (/ (my/beancount--number amount)
                                       (my/beancount--number shares))))
                  price-input))
         (cost-price-raw (read-string "成本价(必填): "))
         (cost-price (my/beancount--normalize-number cost-price-raw))
         (cost-date-input (read-string "成本日期(可空，留空仅按成本价匹配): "))
         (cost-lot (if (string-empty-p cost-date-input)
                       (format "{%s CNY}" cost-price)
                     (format "{%s CNY, %s}" cost-price cost-date-input))))
    (when (string-empty-p cost-price)
      (user-error "银行理财赎回需要成本价，用来匹配 Beancount lot"))
    (my/beancount--append-bank-wealth-entry
     (format "%s * \"赎回 %s (%s)\"\n  product_code: \"%s\"\n  product_name: \"%s\"\n  asset_category: \"银行理财\"\n  trade_price: \"%s CNY\"\n  trade_value: \"%s CNY\"\n  %s  -%s %s %s\n  Assets:Bank:CMB:Cash  %s CNY\n  Income:Investment:CapitalGains\n"
             date name code code name price amount
             account shares commodity cost-lot amount)
     date code)))

(defun my/beancount-record-pension-buy ()
  "快速记录个人养老金基金定投买入。"
  (interactive)
  (let* ((date (my/beancount--read-date))
         (code (let* ((candidates (my/beancount--pension-candidates))
                     (choice (completing-read "养老金基金: " candidates nil t)))
                 (cdr (assoc choice candidates))))
         (fund (my/beancount--pension-fund code))
         (name (plist-get fund :name))
         (commodity (plist-get fund :commodity))
         (account (plist-get fund :account))
         (amount (my/beancount--read-number-string "定投金额: "))
         (shares (my/beancount--read-number-string "成交份额: "))
         (price-input (read-string "成交净值(可空，自动=金额/份额): "))
         (num-amount (my/beancount--number amount))
         (num-shares (my/beancount--number shares))
         (price (if (string-empty-p price-input)
                    (my/beancount--normalize-number
                     (format "%.4f" (/ num-amount num-shares)))
                  price-input))
         (fee (read-string "手续费: " "0")))
    (my/beancount--append-bank-wealth-entry
     (format "%s * \"定投买入 %s (%s)\"\n  fund_code: \"%s\"\n  fund_name: \"%s\"\n  asset_category: \"个人养老金\"\n  trade_price: \"%s CNY\"\n  trade_value: \"%s CNY\"\n  %s  %s %s {{%s CNY}}\n%s  Assets:Bank:CMB:Cash\n"
             date name code code name price amount
             account (my/beancount--normalize-number shares) commodity amount
             (if (and (stringp fee) (> (string-to-number fee) 0))
                 (format "  Expenses:Investment:Fees  %s CNY\n" fee)
               ""))
     date code)))

(defun my/beancount-record-pension-reinvest ()
  "快速记录个人养老金基金红利再投。"
  (interactive)
  (let* ((date (my/beancount--read-date))
         (code (let* ((candidates (my/beancount--pension-candidates))
                     (choice (completing-read "养老金基金: " candidates nil t)))
                 (cdr (assoc choice candidates))))
         (fund (my/beancount--pension-fund code))
         (name (plist-get fund :name))
         (commodity (plist-get fund :commodity))
         (account (plist-get fund :account))
         (shares (my/beancount--read-number-string "红利再投份额: "))
         (price-input (read-string "红利再投净值(可空): "))
         (amount-input (read-string "红利金额(可空，自动=份额*净值): "))
         (amount (cond
                  ((not (string-empty-p amount-input))
                   (my/beancount--normalize-number amount-input))
                  ((not (string-empty-p price-input))
                   (format "%.2f" (* (my/beancount--number shares)
                                     (my/beancount--number price-input))))
                  (t
                   (user-error "红利再投需要填写红利金额，或填写净值用于自动计算"))))
         (price-line (if (string-empty-p price-input)
                         ""
                       (format "  trade_price: \"%s CNY\"\n" price-input))))
    (my/beancount--append-bank-wealth-entry
     (format "%s * \"红利再投 %s (%s)\"\n  fund_code: \"%s\"\n  fund_name: \"%s\"\n  asset_category: \"个人养老金\"\n%s  trade_value: \"%s CNY\"\n  %s  %s %s {{%s CNY}}\n  Income:Investment:Pension:Dividends  -%s CNY\n"
             date name code code name
             price-line amount
             account (my/beancount--normalize-number shares) commodity amount
             amount)
     date code)))

(defun my/beancount-main-file ()
  "Return the main investment Beancount ledger path."
  (expand-file-name "investment/main.beancount" org-directory))

(defun my/open-beancount-main ()
  "打开主 beancount 账本。"
  (interactive)
  (find-file (my/beancount-main-file)))

(defun my/beancount--fund-candidates ()
  "Return completion candidates for `my/beancount-funds`."
  (mapcar (lambda (cell)
            (let* ((code (car cell))
                   (fund (cdr cell))
                   (name (plist-get fund :name)))
              (cons (format "%s · %s" code name) code)))
          my/beancount-funds))

(defun my/beancount--read-fund-code ()
  "Read a fund code from known Beancount fund mappings."
  (let* ((candidates (my/beancount--fund-candidates))
         (choice (completing-read "基金代码: " candidates nil t)))
    (cdr (assoc choice candidates))))

(defun my/beancount--price-candidates ()
  "Return completion candidates for price capture (funds + bank wealth)."
  (append
   (mapcar (lambda (cell)
             (let* ((code (car cell))
                    (fund (cdr cell))
                    (name (plist-get fund :name))
                    (commodity (plist-get fund :commodity)))
               (cons (format "基金 %s · %s" code name)
                     (list :type 'fund :code code :commodity commodity))))
           my/beancount-funds)
   (mapcar (lambda (cell)
             (let* ((code (car cell))
                    (fund (cdr cell))
                    (name (plist-get fund :name))
                    (commodity (plist-get fund :commodity)))
               (cons (format "理财 %s · %s" code name)
                     (list :type 'bank-wealth :code code :commodity commodity))))
           my/beancount-bank-wealth)))

(defun my/beancount--read-price-target ()
  "Read an instrument target for price capture."
  (let* ((candidates (my/beancount--price-candidates))
         (choice (completing-read "价格标的: " candidates nil t)))
    (or (cdr (assoc choice candidates))
        (user-error "Unknown price target: %s" choice))))

(defun my/beancount--fund (code)
  "Return plist for CODE."
  (or (cdr (assoc code my/beancount-funds))
      (user-error "Unknown fund code: %s" code)))

(defun my/beancount--normalize-number (value)
  "Normalize numeric string VALUE for Beancount."
  (replace-regexp-in-string "," "" (string-trim (format "%s" value))))

(defun my/beancount--number (value)
  "Convert VALUE to number after normalizing commas."
  (string-to-number (my/beancount--normalize-number value)))

(defun my/beancount--read-number-string (prompt &optional default)
  "Read a decimal number as normalized string with PROMPT and optional DEFAULT."
  (my/beancount--normalize-number
   (read-string prompt nil nil default)))

(defun my/beancount--read-date (&optional prompt)
  "Read a Beancount date string."
  (read-string (or prompt "交易日期: ") (format-time-string "%Y-%m-%d")))

(defun my/beancount--safe-string (value)
  "Return VALUE trimmed and safe for a Beancount quoted string."
  (replace-regexp-in-string "\"" "'" (string-trim value)))

(defun my/beancount--insert-before-first-entry (text)
  "Insert TEXT before the first price/transaction/balance entry, else at EOF."
  (goto-char (point-min))
  (if (re-search-forward "^[0-9]\{4\}-[0-9]\{2\}-[0-9]\{2\} \\(price\\|balance\\|[*!]\\)" nil t)
      (progn
        (beginning-of-line)
        (insert text "\n"))
    (goto-char (point-max))
    (unless (bolp) (insert "\n"))
    (insert text "\n")))

(defun my/beancount--ensure-fund-open (date code)
  "Ensure commodity and account directives for CODE exist, using DATE if inserted."
  (let* ((fund (my/beancount--fund code))
         (name (plist-get fund :name))
         (commodity (plist-get fund :commodity))
         (account (plist-get fund :account))
         (category (plist-get fund :category))
         directives)
    (save-excursion
      (goto-char (point-min))
      (unless (re-search-forward (format "^[0-9-]+ commodity %s\\b" (regexp-quote commodity)) nil t)
        (push (format "%s commodity %s\n  name: \"%s\"\n  code: \"%s\"\n  asset_category: \"%s\""
                      date commodity name code category)
              directives))
      (goto-char (point-min))
      (unless (re-search-forward (format "^[0-9-]+ open %s\\b" (regexp-quote account)) nil t)
        (push (format "%s open %s %s" date account commodity) directives))
      (when directives
        (my/beancount--insert-before-first-entry
         (mapconcat #'identity (nreverse directives) "\n\n"))))))

(defun my/beancount--append-entry (entry &optional date code)
  "Append ENTRY to main ledger, optionally ensuring CODE is open at DATE."
  (find-file (my/beancount-main-file))
  (when (and date code)
    (my/beancount--ensure-fund-open date code))
  (goto-char (point-max))
  (unless (bolp) (insert "\n"))
  (insert "\n" entry)
  (save-buffer)
  (my/beancount-check-main))

(defun my/beancount-check-main ()
  "Run bean-check against the main Beancount ledger."
  (interactive)
  (let ((file (my/beancount-main-file))
        (program (or (executable-find "bean-check") "bean-check")))
    (save-some-buffers t (lambda ()
                           (and buffer-file-name
                                (file-equal-p buffer-file-name file))))
    (compilation-start
     (format "\"%s\" \"%s\"" program file)
     nil
     (lambda (_) "*bean-check main.beancount*"))))

(defun my/beancount-record-cmb-cashflow ()
  "快速记录招行现金账户的外部流入或流出。"
  (interactive)
  (let* ((date (my/beancount--read-date))
         (direction (completing-read "方向: " '("流入" "流出") nil t))
         (amount (my/beancount--read-number-string "金额: "))
         (summary (my/beancount--safe-string (read-string "摘要: ")))
         (note (my/beancount--safe-string (read-string "备注(可空): ")))
         (title (if (string-empty-p summary)
                    (format "招行现金%s" direction)
                  (format "招行现金%s %s" direction summary)))
         (cash-posting (if (string= direction "流入")
                           (format "  Assets:Bank:CMB:Cash  %s CNY\n" amount)
                         (format "  Assets:Bank:CMB:Cash  -%s CNY\n" amount)))
         (counter-posting (if (string= direction "流入")
                              "  Income:ExternalFunding\n"
                            "  Expenses:ExternalOutflow\n"))
         (note-line (if (string-empty-p note)
                        ""
                      (format "  note: \"%s\"\n" note))))
    (my/beancount--append-entry
     (format "%s * \"%s\"\n  cashflow_type: \"%s\"\n  trade_value: \"%s CNY\"\n%s%s%s"
             date title direction amount note-line cash-posting counter-posting))))

(defun my/beancount-record-broker-transfer ()
  "快速记录招行现金转入股票账户。"
  (interactive)
  (let* ((date (my/beancount--read-date))
         (amount (my/beancount--read-number-string "转入金额: "))
         (summary (my/beancount--safe-string (read-string "摘要: ")))
         (note (my/beancount--safe-string (read-string "备注(可空): ")))
         (title (if (string-empty-p summary)
                    "招行现金转入股票账户"
                  (format "招行现金转入股票账户 %s" summary)))
         (note-line (if (string-empty-p note)
                        ""
                      (format "  note: \"%s\"\n" note))))
    (my/beancount--append-entry
     (format "%s * \"%s\"\n  transfer_type: \"招行现金->股票账户\"\n  trade_value: \"%s CNY\"\n%s  Assets:Investment:Broker:Cash  %s CNY\n  Assets:Bank:CMB:Cash          -%s CNY\n"
             date title amount note-line amount amount))))

(defun my/beancount-record-buy ()
  "快速记录一笔 ETF/基金买入。"
  (interactive)
  (let* ((date (my/beancount--read-date))
         (code (my/beancount--read-fund-code))
         (fund (my/beancount--fund code))
         (name (plist-get fund :name))
         (commodity (plist-get fund :commodity))
         (account (plist-get fund :account))
         (category (plist-get fund :category))
         (price (my/beancount--read-number-string "成交价格: "))
         (shares (my/beancount--read-number-string "成交份额: "))
         (amount (* (my/beancount--number price) (my/beancount--number shares)))
         (fee (if (plist-get fund :fee-free) 0 (* amount 0.00005))))
    (my/beancount--append-entry
     (format "%s * \"买入 %s (%s)\"\n  fund_code: \"%s\"\n  fund_name: \"%s\"\n  asset_category: \"%s\"\n  trade_price: \"%s CNY\"\n  trade_value: \"%.2f CNY\"\n  %s  %s %s {{%.2f CNY}}\n%s  Assets:Investment:Broker:Cash\n"
             date name code code name category price amount
             account shares commodity amount
             (if (> fee 0)
                 (format "  Expenses:Investment:Fees  %.4f CNY\n" fee)
               ""))
     date code)))

(defun my/beancount-record-sell ()
  "快速记录一笔 ETF/基金卖出。"
  (interactive)
  (let* ((date (my/beancount--read-date))
         (code (my/beancount--read-fund-code))
         (fund (my/beancount--fund code))
         (name (plist-get fund :name))
         (commodity (plist-get fund :commodity))
         (account (plist-get fund :account))
         (category (plist-get fund :category))
         (price (my/beancount--read-number-string "成交价格: "))
         (shares (my/beancount--read-number-string "成交份额: "))
         (cost-price (read-string "成本价(可空，例 100.427): "))
         (cost-date (unless (string= cost-price "")
                      (my/beancount--read-date "成本日期: ")))
         (cost-lot (if (string= cost-price "")
                       "{}"
                     (format "{%s CNY, %s}" cost-price cost-date)))
         (trade-value (* (my/beancount--number price) (my/beancount--number shares)))
         (fee (if (plist-get fund :fee-free) 0 (* trade-value 0.00005)))
         (cash-received (- trade-value fee)))
    (my/beancount--append-entry
     (format "%s * \"卖出 %s (%s)\"\n  fund_code: \"%s\"\n  fund_name: \"%s\"\n  asset_category: \"%s\"\n  trade_price: \"%s CNY\"\n  trade_value: \"%.2f CNY\"\n  %s  -%s %s %s @ %s CNY\n%s  Assets:Investment:Broker:Cash  %.2f CNY\n  Income:Investment:CapitalGains\n"
             date name code code name category price trade-value
             account shares commodity cost-lot price
             (if (> fee 0)
                 (format "  Expenses:Investment:Fees  %.2f CNY\n" fee)
               "")
             cash-received)
     date code)))

(defun my/beancount-record-price ()
  "快速记录一条 ETF/基金/理财净值 price。"
  (interactive)
  (let* ((date (my/beancount--read-date "净值日期: "))
         (target (my/beancount--read-price-target))
         (commodity (plist-get target :commodity))
         (price (my/beancount--read-number-string "当前净值: "))
         (ensure-code (when (eq (plist-get target :type) 'fund)
                        (plist-get target :code))))
    (my/beancount--append-entry
     (format "%s price %s %s CNY\n" date commodity price)
     date ensure-code)))

(defun my/beancount-record-balance ()
  "快速记录一条 ETF/基金持仓 balance 校验。"
  (interactive)
  (let* ((date (my/beancount--read-date "校验日期: "))
         (code (my/beancount--read-fund-code))
         (fund (my/beancount--fund code))
         (commodity (plist-get fund :commodity))
         (account (plist-get fund :account))
         (shares (my/beancount--read-number-string "持有份额/股数(不是基金代码): ")))
    (when (string= shares code)
      (user-error "这里要填持有份额/股数，不是基金代码；%s 当前应填实际剩余份额" code))
    (my/beancount--append-entry
     (format "%s balance %s %s %s\n" date account shares commodity)
     date code)))

(defun my/beancount-fava-main ()
  "Open main Beancount ledger and toggle/start Fava."
  (interactive)
  (my/open-beancount-main)
  (if (fboundp 'beancount-fava)
      (beancount-fava)
    (start-process "fava" "*fava*" "fava" (my/beancount-main-file))))

(defun my/beancount-python-executable ()
  "Return a Python executable with the investment script dependencies."
  (let ((candidates (list (expand-file-name "scoop/apps/python311/current/python.exe" "~")
                          (expand-file-name "scoop/apps/python314/current/python.exe" "~")
                          (executable-find "python"))))
    (catch 'found
      (dolist (candidate candidates)
        (when (and candidate (file-executable-p candidate))
          (throw 'found candidate)))
      "python")))

(defun my/beancount-update-prices-latest ()
  "抓取最近两周 ETF 收盘价，更新 prices.beancount。"
  (interactive)
  (let* ((script (expand-file-name "investment/scripts/update_prices.py" org-directory))
         (default-directory (expand-file-name "investment/" org-directory)))
    (compilation-start
     (format "%s %s latest"
             (shell-quote-argument (my/beancount-python-executable))
             (shell-quote-argument script))
     nil
     (lambda (_) "*beancount update prices latest*"))))

(defun my/beancount-rebuild-prices-history ()
  "重建全部 ETF 历史收盘价 prices.beancount；建议每周跑一次。"
  (interactive)
  (let* ((script (expand-file-name "investment/scripts/update_prices.py" org-directory))
         (default-directory (expand-file-name "investment/" org-directory)))
    (compilation-start
     (format "%s %s history"
             (shell-quote-argument (my/beancount-python-executable))
             (shell-quote-argument script))
     nil
     (lambda (_) "*beancount rebuild prices history*"))))

(map! :leader
      (:prefix ("n i" . "investment · 投资")
       :desc "Beancount buy · 买入"                  "b" #'my/beancount-record-buy
       :desc "Beancount sell · 卖出"                 "s" #'my/beancount-record-sell
       :desc "Beancount price · 记录价格"            "p" #'my/beancount-record-price
       :desc "Beancount balance · 持仓校验"          "B" #'my/beancount-record-balance
       :desc "Update prices latest · 更新近期价格"   "u" #'my/beancount-update-prices-latest
       :desc "Rebuild price history · 重建历史价格"  "H" #'my/beancount-rebuild-prices-history
       :desc "Open ledger · 打开账本"                "o" #'my/open-beancount-main
       :desc "Check ledger · 检查账本"               "c" #'my/beancount-check-main
       :desc "Fava · 打开报表"                       "f" #'my/beancount-fava-main
       :desc "Bank wealth redeem · 银行理财赎回"     "r" #'my/beancount-record-bank-redeem
       :desc "Pension fund buy · 养老金基金买入"     "y" #'my/beancount-record-pension-buy
       :desc "Pension reinvest · 养老金红利再投"     "R" #'my/beancount-record-pension-reinvest
       :desc "Pension reinvest · 养老金红利再投"     "Y" #'my/beancount-record-pension-reinvest
       :desc "CMB cashflow · 招行现金流入流出"       "C" #'my/beancount-record-cmb-cashflow
       :desc "CMB to broker · 招行转入股票账户"      "t" #'my/beancount-record-broker-transfer))
#+end_src

* Apps
** Eshell
#+begin_src emacs-lisp
(defun my/eshell-here ()
  "Open a new eshell rooted at the current buffer's directory."
  (interactive)
  (let ((default-directory (or (and buffer-file-name
                                    (file-name-directory buffer-file-name))
                               default-directory)))
    (eshell t)))

(defun eshell/doom (&rest args)
  "Run Doom Emacs CLI from eshell.
Invokes the current Emacs executable in batch mode with the doom script,
identical to what `doom.ps1' does on Windows."
  (let ((doom-script (expand-file-name "~/.emacs.d/bin/doom"))
        (emacs-bin (expand-file-name invocation-name invocation-directory)))
    (if (file-exists-p doom-script)
        (apply #'call-process emacs-bin nil t nil
               "-q" "--no-site-file" "--batch"
               "--load" doom-script "--" "--no-color"
               args)
      (error "Doom script not found at %s" doom-script))))
#+end_src

** Elfeed
#+begin_src emacs-lisp
(use-package! elfeed
  :commands elfeed
  :config
  (require 'subr-x)
  (setq elfeed-db-directory (expand-file-name "~/org/.elfeed")
        elfeed-curl-max-connections 4)
  (setq-default elfeed-search-filter "@1-month-ago +unread")
  (add-hook 'elfeed-search-mode-hook #'elfeed-update)

  (defun my/elfeed-show-buffer-setup ()
    "Apply local display settings for Elfeed article buffers."
    (setq-local shr-use-fonts nil)
    (setq-local line-spacing 0))

  (defun my/elfeed-select-show-window (&rest _)
    "Keep focus in the Elfeed article pane after show-buffer navigation."
    (when-let ((window (cl-find-if
                        (lambda (window)
                          (with-current-buffer (window-buffer window)
                            (derived-mode-p 'elfeed-show-mode)))
                        (window-list nil 'no-minibuf))))
      (select-window window)))

  (add-hook 'elfeed-show-mode-hook #'my/elfeed-show-buffer-setup)
  (unless (advice-member-p #'my/elfeed-select-show-window 'elfeed-show-next)
    (advice-add 'elfeed-show-next :after #'my/elfeed-select-show-window))
  (unless (advice-member-p #'my/elfeed-select-show-window 'elfeed-show-prev)
    (advice-add 'elfeed-show-prev :after #'my/elfeed-select-show-window))

  ;; Avoid expensive reload/save on exit when Elfeed DB is not initialized.
  (defun my/elfeed-db-save-if-initialized (orig-fn &rest args)
    (cond
     ((null elfeed-db)
      (message "Skip elfeed-db-save: elfeed-db is not initialized in this session."))
     ((not (plistp elfeed-db))
      (message "Skip elfeed-db-save: invalid elfeed-db (%S)." elfeed-db))
     (t
      (apply orig-fn args))))
  (when (advice-member-p #'my/elfeed-db-sanitize-before-save 'elfeed-db-save)
    (advice-remove 'elfeed-db-save #'my/elfeed-db-sanitize-before-save))
  (unless (advice-member-p #'my/elfeed-db-save-if-initialized 'elfeed-db-save)
    (advice-add 'elfeed-db-save :around #'my/elfeed-db-save-if-initialized))
  (remove-hook 'kill-emacs-hook #'elfeed-db-gc-safe)

  (defvar my/elfeed-feed-history nil
    "Minibuffer history for feed selection commands.")

  (defun my/elfeed-current-entry ()
    "Return the current Elfeed entry in search or show buffers."
    (cond
     ((derived-mode-p 'elfeed-show-mode) elfeed-show-entry)
     ((derived-mode-p 'elfeed-search-mode) (elfeed-search-selected :ignore-region))
     (t nil)))

  (defun my/elfeed-feed-display-title (feed)
    (or (elfeed-meta feed :title)
        (elfeed-feed-title feed)
        (elfeed-feed-url feed)
        "Unknown feed"))

  (defun my/elfeed-url-origin (url)
    (when (and url (string-match "\\`\\(https?://[^/]+\\)" url))
      (concat (match-string 1 url) "/")))

  (defun my/elfeed-guess-homepage (feed-url)
    (when feed-url
      (let ((homepage (car (split-string feed-url "[?#]" t))))
        (dolist (pattern '("/index\\.xml/?$"
                           "/atom\\.xml/?$"
                           "/rss\\.xml/?$"
                           "/feed\\.xml/?$"
                           "/feeds?/[^/]+\\(?:\\.xml\\|\\.atom\\|\\.rss\\)?/?$"
                           "/\\(?:feed\\|rss\\|atom\\)/?$"))
          (setq homepage (replace-regexp-in-string pattern "/" homepage t t)))
        (unless (string-match-p "/$" homepage)
          (setq homepage
                (replace-regexp-in-string "/[^/]+\\(?:\\.xml\\|\\.rss\\|\\.atom\\)?$"
                                          "/"
                                          homepage
                                          t
                                          t)))
        (replace-regexp-in-string "/+$" "/" homepage t t))))

  (defun my/elfeed-entry-homepage (entry)
    (when-let* ((feed (elfeed-entry-feed entry))
                (feed-url (elfeed-feed-url feed)))
      (let* ((entry-url (elfeed-entry-link entry))
             (feed-origin (my/elfeed-url-origin feed-url))
             (entry-origin (my/elfeed-url-origin entry-url)))
        (cond
         ((and entry-origin feed-origin (not (string= entry-origin feed-origin)))
          entry-origin)
         (t
          (or (my/elfeed-guess-homepage feed-url)
              entry-origin
              feed-origin))))))

  (defun my/elfeed-browse-homepage ()
    "Open the homepage of the current entry's feed."
    (interactive)
    (if-let ((entry (my/elfeed-current-entry)))
        (if-let ((homepage (my/elfeed-entry-homepage entry)))
            (browse-url homepage)
          (user-error "Could not infer a homepage for this feed"))
      (user-error "No Elfeed entry at point")))

  (defun my/elfeed-toggle-unread ()
    "Toggle the `unread' tag on the current Elfeed entry or entries."
    (interactive)
    (cond
     ((derived-mode-p 'elfeed-search-mode)
      (elfeed-search-toggle-all 'unread))
     ((derived-mode-p 'elfeed-show-mode)
      (if (elfeed-tagged-p 'unread elfeed-show-entry)
          (elfeed-show-untag 'unread)
        (elfeed-show-tag 'unread)))
     (t
      (user-error "Not in an Elfeed buffer"))))

  (defun my/elfeed-feed-candidates ()
    (let ((table (make-hash-table :test 'equal)))
      (with-elfeed-db-visit (entry feed)
        (let* ((url (elfeed-feed-url feed))
               (title (my/elfeed-feed-display-title feed))
               (current (or (gethash url table)
                            (list :url url :title title :all 0 :unread 0)))
               (all (1+ (plist-get current :all)))
               (unread (if (elfeed-tagged-p 'unread entry)
                           (1+ (plist-get current :unread))
                         (plist-get current :unread))))
          (puthash url
                   (list :url url :title title :all all :unread unread)
                   table)))
      (sort
       (let (candidates)
         (maphash
          (lambda (url data)
            (push (cons (format "[%d/%d] %s\t%s"
                                (plist-get data :unread)
                                (plist-get data :all)
                                (plist-get data :title)
                                url)
                        url)
                  candidates))
          table)
         candidates)
       (lambda (a b) (string-lessp (car a) (car b))))))

  (defun my/elfeed-read-feed-url ()
    (let* ((all-feeds "[All feeds]")
           (candidates (my/elfeed-feed-candidates))
           (choices (cons all-feeds (mapcar #'car candidates)))
           (choice (if (require 'consult nil t)
                       (consult--read
                        choices
                        :prompt "Feed: "
                        :sort nil
                        :require-match t
                        :history 'my/elfeed-feed-history)
                     (completing-read "Feed: " choices nil t nil 'my/elfeed-feed-history))))
      (unless (equal choice all-feeds)
        (cdr (assoc choice candidates)))))

  (defun my/elfeed-replace-feed-filter (filter feed-url)
    (let ((parts (cl-remove-if (lambda (part) (string-prefix-p "=" part))
                               (split-string (string-trim (or filter "")) "[ \t\n]+" t))))
      (string-join
       (append parts
               (when feed-url
                 (list (concat "=" (regexp-quote feed-url)))))
       " ")))

  (defun my/elfeed-search-filter-by-feed ()
    "Use `consult' or `completing-read' to filter Elfeed by feed."
    (interactive)
    (unless (derived-mode-p 'elfeed-search-mode)
      (user-error "Feed filtering is only available in elfeed-search"))
    (let* ((feed-url (my/elfeed-read-feed-url))
           (new-filter (my/elfeed-replace-feed-filter elfeed-search-filter feed-url)))
      (elfeed-search-set-filter new-filter)
      (message (if feed-url
                   "Filtered Elfeed to %s"
                 "Cleared feed-specific filter")
               (or feed-url "all feeds"))))

  ;; mpv integration
  (defun my/elfeed-play-with-mpv ()
    "Play the current elfeed entry with mpv."
    (interactive)
    (let ((link (if (derived-mode-p 'elfeed-show-mode)
                    (elfeed-entry-link elfeed-show-entry)
                  (let ((entries (elfeed-search-selected)))
                    (when entries
                      (elfeed-entry-link (car entries)))))))
      (if link
          (progn
            (message "Starting mpv for %s..." link)
            (start-process "elfeed-mpv" nil "mpv" link)
            (when (derived-mode-p 'elfeed-search-mode)
              (elfeed-search-untag-all-unread)))
        (message "No link found."))))

  (map! :map elfeed-search-mode-map
        :desc "Filter by feed"      "C-c f" #'my/elfeed-search-filter-by-feed
        :desc "Browse feed homepage" "B" #'my/elfeed-browse-homepage
        :desc "Toggle unread"       "R" #'my/elfeed-toggle-unread
        :desc "Play with mpv"       "v" #'my/elfeed-play-with-mpv)
  (map! :map elfeed-show-mode-map
        :desc "Browse feed homepage" "B" #'my/elfeed-browse-homepage
        :desc "Toggle unread"       "R" #'my/elfeed-toggle-unread
        :desc "Play with mpv"       "v" #'my/elfeed-play-with-mpv))

(use-package! elfeed-org
  :after elfeed
  :config
  (elfeed-org)
  (setq rmh-elfeed-org-files (list (expand-file-name "~/org/notes/20260101T000010--elfeed__emacs_index.org"))))

(after! elfeed-goodies
  (setq elfeed-goodies/entry-pane-position 'right
        elfeed-goodies/entry-pane-size 0.55
        elfeed-goodies/switch-to-entry t
        elfeed-goodies/feed-source-column-width 28
        elfeed-goodies/tag-column-width 18))

(after! elfeed-tube
  (setq elfeed-tube-auto-fetch-p t
        elfeed-tube-auto-save-p nil
        elfeed-tube-fields '(duration captions chapters description)
        elfeed-tube-backend 'yt-dlp))
#+end_src

** Agent-shell
=my/athenai-api-key= 由 =secrets.el= 提供; 缺失时 agent-shell 配置静默跳过。
#+begin_src emacs-lisp
(when (boundp 'my/athenai-api-key)
  (require 'acp)
  (require 'agent-shell)

  (setq agent-shell-preferred-agent-config
        (agent-shell-anthropic-make-claude-code-config))

  (setq agent-shell-anthropic-authentication
        (agent-shell-anthropic-make-authentication
         :api-key (lambda () my/athenai-api-key)))

  (setq agent-shell-anthropic-claude-environment
        (agent-shell-make-environment-variables
         "ANTHROPIC_BASE_URL" "https://athenai.mihoyo.com/v1"
         "ANTHROPIC_MODEL" "claude-sonnet-4-6"
         "ANTHROPIC_SMALL_FAST_MODEL" "claude-sonnet-4-6"))

  ;; 统一 transcript 存储位置, 无论在哪个 project 下都存到 ~/.agent-shell/
  (setq agent-shell-dot-subdir-function
        (lambda (subdir)
          (expand-file-name subdir "~/.agent-shell")))

  ;; Buffer-local Evil-friendly bindings
  ;; NOTE: comint-mode-map 绑了 RET → comint-send-input，shell-maker-mode-map 又用 <remap>
  ;; 转成 shell-maker-submit。evil-define-key 'insert 在 insert state 下不一定能拦截到 RET
  ;; （insert state 设计透明，大部分键穿透到 mode map 链）。
  ;;
  ;; 解法：直接在 agent-shell-mode-map 上绑 RET → newline 截断穿透链；normal state 下
  ;; 用 evil-define-key 单独绑 RET → shell-maker-submit。
  (after! agent-shell
    (define-key agent-shell-mode-map (kbd "RET")   #'newline)
    (define-key agent-shell-mode-map (kbd "S-RET") #'shell-maker-submit)
    (evil-define-key 'normal agent-shell-mode-map
      (kbd "RET") #'shell-maker-submit
      "q"         #'quit-window)
    (evil-define-key 'normal agent-shell-mode-map
      (kbd "gr") #'agent-shell-send-dwim)
    (evil-define-key 'visual agent-shell-mode-map
      (kbd "gr") #'agent-shell-send-dwim))

  ;; Diff buffer auto enters emacs-state
  (add-hook 'diff-mode-hook
            (lambda ()
              (when (string-match-p "\\*agent-shell-diff\\*" (buffer-name))
                (evil-emacs-state))))
  ;; Viewport auto emacs-state
  (with-eval-after-load 'evil
    (evil-set-initial-state 'agent-shell-viewport-view-mode 'emacs)))
#+end_src

* Languages
** Markdown
#+begin_src emacs-lisp
(after! markdown-mode
  (setq markdown-command "pandoc"
        markdown-open-command
        (when (featurep :system 'windows) "explorer.exe")
        markdown-fontify-code-blocks-natively t
        markdown-header-scaling t
        markdown-enable-wiki-links t
        markdown-italic-underscore t
        markdown-asymmetric-header nil
        markdown-live-preview-delete-export 'delete-on-destroy))
#+end_src

** PowerShell
#+begin_src emacs-lisp
(use-package! powershell
  :mode ("\\.ps1\\'" . powershell-mode)
  :config
  (defun my/powershell-run-file ()
    "Run current .ps1 file."
    (interactive)
    (unless buffer-file-name (user-error "Buffer has no file"))
    (save-buffer)
    (compile (format "pwsh -NoProfile -ExecutionPolicy Bypass -File \"%s\""
                     (expand-file-name buffer-file-name))))

  (defun my/powershell-run-region ()
    "Send region or current line to inferior PowerShell."
    (interactive)
    (let* ((beg (if (use-region-p) (region-beginning) (line-beginning-position)))
           (end (if (use-region-p) (region-end)       (line-end-position)))
           (code (buffer-substring-no-properties beg end)))
      (unless (get-buffer "*PowerShell*") (powershell))
      (comint-send-string (get-buffer-process "*PowerShell*") (concat code "\n"))
      (display-buffer "*PowerShell*")))

  (map! :map powershell-mode-map
        "C-c C-c" #'my/powershell-run-file
        "C-c C-r" #'my/powershell-run-region
        "C-c C-z" #'powershell))
#+end_src

* Keybindings
** Leader
#+begin_src emacs-lisp
(map! :leader
      ;; Gcal
      (:prefix ("G" . "gcal")
       :desc "Sync"   "s" #'org-gcal-sync
       :desc "Fetch"  "f" #'org-gcal-fetch
       :desc "Delete" "d" #'org-gcal-delete-at-point
       :desc "Push"   "p" #'my/org-gcal-auto-post)

      ;; Open
      (:prefix ("o" . "open")
       :desc "Elfeed"           "e" #'elfeed
       :desc "Eshell here"      "E" #'my/eshell-here
       :desc "Claude Code"      "s" #'agent-shell-anthropic-start-claude-code
       :desc "Codex"            "c" #'agent-shell-openai-start-codex
       :desc "Daily agenda"     "d" (cmd! (org-agenda nil "d"))
       :desc "Weekly agenda"    "w" (cmd! (org-agenda nil "w"))
       :desc "GTD review"       "g" (cmd! (org-agenda nil "g"))
       :desc "Beancount ledger" "$" #'my/open-beancount-main)

      ;; Notes / Denote
      (:prefix ("n" . "notes")
       :desc "Consult notes (pick)"   "s" #'consult-notes
       :desc "Consult notes (search)" "S" #'consult-notes-search-in-all-notes
       (:prefix ("d" . "denote")
        :desc "New note"                       "n" #'denote
        :desc "New note (choose type)"         "N" #'denote-type
        :desc "New note in subdir"             "D" #'denote-subdirectory
        :desc "Open or create"                 "f" #'denote-open-or-create
        :desc "Insert link"                    "i" #'denote-link-or-create
        :desc "Insert link (region → title)"   "I" #'denote-link
        :desc "Add links (search)"             "L" #'denote-add-links
        :desc "Backlinks"                      "b" #'denote-backlinks
        :desc "Rename file"                    "r" #'denote-rename-file
        :desc "Rename from front-matter"       "R" #'denote-rename-file-using-front-matter
        :desc "Add keywords"                   "k" #'denote-keywords-add
        :desc "Remove keywords"                "K" #'denote-keywords-remove
        :desc "Dired in denote dir"            "d" #'denote-dired
        :desc "Today's journal"                "j" #'denote-journal-new-or-existing-entry))

      ;; Toggle / utilities
      (:prefix ("t" . "toggle")
       ;; :desc "Focus writing"         "F" #'my/focus-writing-toggle
       )

      :desc "Visual undo tree" "u" #'vundo
      :desc "org-noter"        "N" #'org-noter)
#+end_src

** Org localleader
#+begin_src emacs-lisp
(map! :after org
      :map org-mode-map
      :localleader
      (:prefix ("i" . "image")
       :desc "Toggle inline"  "t" #'org-toggle-inline-images
       :desc "Paste clipboard" "p" #'org-download-clipboard
       :desc "Screenshot"     "s" #'org-download-screenshot
       :desc "Yank"           "y" #'org-download-yank
       :desc "Delete file"   "d" #'my/org-delete-file-link-at-point
       :desc "Rotate file"  "r" #'my/org-rotate-image-file)
      (:prefix ("d" . "denote")
       :desc "Insert link"            "i" #'denote-link-or-create
       :desc "Backlinks"              "b" #'denote-backlinks
       :desc "Add keywords"           "k" #'denote-keywords-add
       :desc "Rename (front-matter)"  "r" #'denote-rename-file-using-front-matter
       :desc "Link to heading"        "h" #'denote-org-link-to-heading
       :desc "Backlinks for heading"  "H" #'denote-org-backlinks-for-heading
       :desc "Extract subtree to note" "x" #'denote-org-extract-org-subtree
       (:prefix ("o" . "dblock")
        :desc "Links (by regexp)"     "l" #'denote-org-dblock-insert-links
        :desc "Backlinks"             "b" #'denote-org-dblock-insert-backlinks
        :desc "Missing links"         "m" #'denote-org-dblock-insert-missing-links
        :desc "Files"                 "f" #'denote-org-dblock-insert-files
        :desc "Files as headings"     "F" #'denote-org-dblock-insert-files-as-headings))
      ;; org-transclusion
      (:prefix ("T" . "transclusion")
       :desc "Add at point"    "a" #'org-transclusion-add
       :desc "Add all"         "A" #'org-transclusion-add-all
       :desc "Remove at point" "r" #'org-transclusion-remove
       :desc "Toggle mode"     "t" #'org-transclusion-mode)
      :desc "TODO state"         "t" #'org-todo
      :desc "Paste rich text"    "V" #'my/org-paste-rich
      :desc "Archive done tasks" "A" #'my/org-archive-done-tasks)
#+end_src

** Markdown localleader
#+begin_src emacs-lisp
(map! :after markdown-mode
      :map markdown-mode-map
      :localleader
      (:prefix ("d" . "denote")
       :desc "Insert link"              "i" #'denote-link-or-create
       :desc "Backlinks"                "b" #'denote-backlinks
       :desc "Rename (front-matter)"    "r" #'denote-rename-file-using-front-matter
       :desc "denote: → file path"      "f" #'denote-markdown-convert-links-to-file-paths
       :desc "file path → denote:"      "F" #'denote-markdown-convert-links-to-denote-type
       :desc "denote: → Obsidian"       "o" #'denote-markdown-convert-links-to-obsidian-type
       :desc "Obsidian → denote:"       "O" #'denote-markdown-convert-obsidian-links-to-denote-type)
      (:prefix ("i" . "image")
       :desc "Delete file link" "d" #'my/markdown-delete-file-link-at-point))
#+end_src

* Late fixes
** C-c C-c in =*Org Note*= popup
#+begin_src emacs-lisp
(defadvice! my/org-ctrl-c-ctrl-c-note-fix (fn &rest args)
  :around #'org-ctrl-c-ctrl-c
  (if (string= (buffer-name) "*Org Note*")
      (org-store-log-note)
    (apply fn args)))
#+end_src

** Smart Input Source (sis) + Rime — Windows
#+begin_src emacs-lisp
(when (featurep :system 'windows)
  (use-package! sis
    :demand t
    :config
    ;; Skip sis's auto-detect; wire Windows IME API directly.
    (setq sis-english-source nil
          sis-other-source   t
          sis-do-get         #'w32-get-ime-open-status
          sis-do-set         #'w32-set-ime-open-status)

    ;; Disable 0.2s idle poll (races imm32.dll on buffer creation).
    (setq sis-auto-refresh-seconds nil)

    (defun sis-switch ()
      "Switch between English and other, driven by the Windows IME API."
      (interactive)
      (setq sis--for-buffer-locked nil)
      (if (w32-get-ime-open-status)
          (sis--set-english)
        (sis--set-other)))

    (sis-global-cursor-color-mode t)
    (when (fboundp 'modus-themes-get-color-value)
      (setq sis-other-cursor-color   (modus-themes-get-color-value 'fg-alt)
            sis-default-cursor-color (modus-themes-get-color-value 'border)))

    (sis-global-respect-mode t)
    (sis-global-context-mode t)
    (sis-global-inline-mode  t)

    (setq sis-respect-go-english-triggers (list #'org-agenda))

    ;; Resync after frame regains focus
    (add-function :after after-focus-change-function
                  (lambda () (sis--get)))

    :hook
    (org-capture-mode . sis-set-other))

  ;; Bind C-\ everywhere evil might grab keys first.
  (define-key global-map (kbd "C-\\") #'sis-switch)
  (with-eval-after-load 'evil
    (define-key evil-normal-state-map (kbd "C-\\") #'sis-switch)
    (define-key evil-insert-state-map (kbd "C-\\") #'sis-switch)
    (define-key evil-motion-state-map (kbd "C-\\") #'sis-switch)
    (define-key evil-visual-state-map (kbd "C-\\") #'sis-switch)
    (define-key evil-emacs-state-map  (kbd "C-\\") #'sis-switch)))
#+end_src
